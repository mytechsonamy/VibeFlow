{
  "name": "@vibeflow/dev-ops",
  "version": "0.1.0",
  "description": "VibeFlow dev-ops MCP server \u2014 CI/CD integration (GitHub Actions / GitLab CI), pipeline trigger, status polling, artifact listing, deploy + rollback wrappers.",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "vibeflow-dev-ops": "./dist/index.js"
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc && esbuild dist/index.js --bundle --platform=node --format=esm --outfile=dist/bundle.js --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\"",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "@vitest/coverage-v8": "^2.1.9",
    "esbuild": "^0.28.0",
    "typescript": "^5.6.2",
    "vitest": "^2.1.2"
  }
}
