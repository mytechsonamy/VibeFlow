{
  "name": "@vibeflow/design-bridge",
  "version": "0.1.0",
  "description": "VibeFlow design-bridge MCP server — Figma REST client, frame + token extraction, style generation.",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "vibeflow-design-bridge": "./dist/index.js"
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "typescript": "^5.6.2",
    "vitest": "^2.1.2"
  }
}
