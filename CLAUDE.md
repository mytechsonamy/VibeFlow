# VibeFlow - AI-Orchestrated Vibe Coding Framework

## Project Overview
VibeFlow is a Claude Code plugin that orchestrates the full SDLC through multi-AI consensus and truth validation. It combines MyVibe Framework's orchestration with TruthLayer's requirements-first quality assurance.

## Architecture Principles
- **Plugin-First**: All components are Claude Code native (skills, hooks, subagents, MCP servers)
- **Truth-Driven**: Requirements are the source of truth. Testability score < 60 blocks development
- **Consensus-Based**: Multi-AI review (Claude + ChatGPT + Gemini) with graceful degradation
- **Single Backend**: File-backed state store under `.vibeflow/state/<project>/` (`project.json` rollup + append-only `events/` + per-phase `archive/`). Git provides versioning for free. Consensus reviewer count is driven by which external CLIs are on PATH (codex/gemini), not by a mode switch.

## SDLC Phases (7)
1. REQUIREMENTS - PRD analysis, testability scoring, ambiguity detection
2. DESIGN - Figma integration, wireframes, design tokens, accessibility
3. ARCHITECTURE - ADR generation, tech decisions, 3-AI voting, formal verification
4. PLANNING - Epic breakdown, task dependency graph, test strategy, sprint planning
5. DEVELOPMENT - Code with auto quality gates (hooks), brownfield context management
6. TESTING - Full TruthLayer validation (20 skills), mutation testing, chaos injection
7. DEPLOYMENT - Release decision (GO/CONDITIONAL/BLOCKED), rollback, health checks

## Consensus Thresholds
- APPROVED: >= 90% agreement + 0 critical issues
- NEEDS_REVISION: 50-89% agreement
- REJECTED: < 50% or 2+ critical issues
- Always use enum values (ConsensusStatus.REJECTED), never string literals

## Domain-Specific Quality Thresholds
- Financial: GO >= 90, CONDITIONAL >= 75 (invariants weighted 25%)
- E-commerce: GO >= 85, CONDITIONAL >= 70 (UAT weighted 25%)
- Healthcare: GO >= 95, CONDITIONAL >= 85 (coverage weighted 30%)
- General: GO >= 80, CONDITIONAL >= 65

## Key Commands
- `/vibeflow:onboard` - Onboard a new project (domain, platform, tech stack). Renamed from `/vibeflow:init` in v2.19.0 to avoid colliding with Claude Code's built-in `/init`.
- `/vibeflow:flow-status` - Current SDLC phase, pending tasks, quality metrics. Renamed from `/vibeflow:status` in v2.20.0 to avoid colliding with Claude Code's built-in `/status` (usage/quota).
- `/vibeflow:consensus-orchestrator` - Trigger the multi-AI review cycle (Claude + codex + gemini)
- `/vibeflow:advance` - Advance to next SDLC phase (with gate checks)
- `/vibeflow:prd-quality-analyzer` - Analyze PRD quality
- `/vibeflow:test-strategy-planner` - Generate test strategy from PRD
- `/vibeflow:release-decision-engine` - Produce release decision
- `/vibeflow:streams` - Parallel team-work dashboard (Sprint 60; meaningful when `streams.enabled` is on — per-branch isolated SDLC state). See `docs/TEAM-WORK.md`.
- `/vibeflow:integrate` - The merge point for parallel work-streams: combined front↔back verify + cross-feature consensus on the integration branch (Sprint 60).

## Build & Test
- MCP servers: `cd mcp-servers/<server> && npm install && npm run build`
- Plugin dev mode: `claude --plugin-dir ./` from VibeFlow root

### Nineteen test layers — run all nineteen before declaring a sprint ticket done
- **Unit (vitest) — 5 MCP servers, each with 80/80/80/80 coverage threshold:**
  - `cd mcp-servers/sdlc-engine && npm test` — 206 tests (engine, consensus, validation, phases, filesystem state store, tools, server dispatch, Sprint 60 StreamsConfigSchema default-off [enabled:false] + idStrategy branch|fixed enum + EngineConfigSchema wiring + partial-merge, Sprint 49 satisfyCriterion HTML-entity normalization (normalizeCriterion), Sprint 48 sdlc_start_cycle reset [REQUIREMENTS + clears criteria/consensus + bumps cycle], Bug #13 getOrInit fast-path, filesystem race tests incl. 5-process cross-process lock + 50-concurrent-writer Bug #6 regression, event model round-trips, Sprint 15-C phase-scoped consensus validation, Sprint 17-C HUMAN_APPROVAL_REQUIRED validator pass-through, Sprint 17-F ConsensusConfigSchema defaults + partial merge, Sprint 19-G PhaseRunnerConfigSchema, Sprint 20-H LearningLoopConfigSchema + ReviewerMemoryConfigSchema defaults + partial merge, Sprint 21-E ConsensusConfig excerptTokenBudget/excerptStrategy + ReviewerMemory compaction defaults, Sprint 22-D LearningApplyConfigSchema defaults + partial merge + range rejection, Sprint 23-A AutoApplyConfigSchema default-OFF + nested partial-merge + regressionDelta range rejection, Sprint 24 demoteAfterReverts + transactional defaults + range rejection, Sprint 25-C LoopAuditConfigSchema window default + range + partial-merge, Sprint 26-B GatesConfigSchema default-off + validator target-entryCriteria gate (off/on/satisfied/force) + TESTING/DEPLOYMENT phases criteria contract, Sprint 27-A autoForkSpecialist default-off + partial-merge, Sprint 28-A GlobalLearningConfigSchema default-off + partial-merge, Sprint 33 ConsensusConfig cliTimeoutSeconds default 90 + range rejection)
  - `cd mcp-servers/codebase-intel && npm test` — 48 tests (structure, dependency graph, hotspots, tech debt, large-input scaling)
  - `cd mcp-servers/design-bridge && npm test` — 57 tests (figma fetch, token extract, style generate, compare, offline / network failure)
  - `cd mcp-servers/dev-ops && npm test` — 72 tests (GitHub client + pipelines + CI_PROVIDER selection + GitLab client 28 cases including 9 self-hosted baseUrl cases from S7-01 + tools-layer GITLAB_BASE_URL plumbing)
  - `cd mcp-servers/observability && npm test` — 76 tests (metric collect, flaky track, perf trend, health dashboard, parser edge branches)
- **Hook scripts (bash):** `bash hooks/tests/run.sh` — 213 assertions covering every hook + shared `_lib.sh` + S4-02 hardening + S4-08 output budgets + S11-C filesystem-first helpers + S13-A phase-write-guard decisions + S14-A/B consensus CLI-gating + non-reviewer filtering + S16-A consensus-gate marker/pass-through/bypass + S17-A round-aware aggregator + S17-B critical dedup + Jaccard title similarity + S17-C load-sdlc-context override advisory + S19-PLAN save-plan-doc hook (sprint-numbered title + fallback + guards) + S20-A consensus history.jsonl append + idempotency + S20-D reviewer-memory per-artifact cap + S20-F consensus-gate primaryArtifact (v2 + legacy fallback) + S21-B reviewer-memory theme-aware compaction (summary fold + seenCount + recency opt-out) + S23-C auto-apply revert-watch (regression restores snapshot + cooldown + history auto-revert row / hold / no-watch no-op) + S30-A loop-audit phaseRunner (ledger step counts) + crossProject (globalLearning-gated cross-project aggregation; null when off/absent) + S60-A vf_stream_id (off→bare / on+feature→`<project>__<slug>` / on+main→bare / detached→bare / VIBEFLOW_STREAM override) + vf_state_project_dir stream-scoped + S60-B vf_lifecycle_path (off→legacy / on+feature→stream-scoped / on+main→legacy) + stream-id.sh JSON reader + two-branch state isolation + S62-A consensus-gate cd-preamble bypass (cd;/cd&&/quoted-path/double-cd + leading-bypass → allow; cd&&quoted-mention / non-cd-work&&bypass / plain-write → block)
- **Integration (bash + node):**
  - `bash tests/integration/run.sh` — 398 assertions (platform baseline: plugin manifest, hooks.json, .mcp.json dist paths, 5 MCP stdio smokes, engine+hook e2e against filesystem backend, Sprint-2 + Sprint-3 skill structural + gate-contract sentinels, Bug #13 cross-process reproducer against project.json)
  - `bash tests/integration/sprint-2.sh` — 94 assertions (Sprint 2 L1 skill coherence + io-standard + MCP sanity + bug closure)
  - `bash tests/integration/sprint-3.sh` — 111 assertions (Sprint 3 L1/L2/L3 skill inventory + cross-skill wiring + gate contracts + PIPELINE coverage + bug closure)
  - `bash tests/integration/sprint-4.sh` — 352 assertions (coverage + io-standard + demo-app + user docs + plugin manifest + ci_provider wiring + packaging + tarball + CHANGELOG sync + offline/large-input/budget sentinels + fresh-install end-to-end simulation [S4-K] against project.json + archive layout + S7-01 gitlab_token/gitlab_base_url userConfig keys + S9-07 version-matched tarball lookup)
  - `bash tests/integration/sprint-5.sh` — 97 assertions (GitLab CI provider wiring [S5-A] + release script + workflow + CHANGELOG runtime sentinel [S5-C] + Next.js demo layout + artifacts [S5-D] + Bug #13 cross-process reproducer mirror [S5-E] against project.json). [S5-B] live-PG walk retired in Sprint 11-E.
  - `bash tests/integration/sprint-6.sh` — 37 assertions: Next.js demo "use client" surface + optional `next build` gate [S6-B] + GPG-signed release tags + RELEASING.md walkthrough [S6-C] + sprint-6.sh harness self-audit [S6-Z]. [S6-A] concurrent-CAS PG stress test retired in Sprint 11-E.
  - `bash tests/integration/sprint-7.sh` — 35 assertions: release.sh pg peer-dep sanity check [S7-A] + RELEASING.md troubleshooting + sha256-drift recovery [S7-B] + reproducible package-plugin.sh tarball [S7-C] + self-hosted GitLab baseUrl plumbing [S7-D] + sprint-7.sh harness self-audit [S7-Z]. [S7-E] PG version matrix retired in Sprint 11-E.
  - `bash tests/integration/sprint-8.sh` — 33 assertions: sprint-7.sh [S7-C] multi-tarball save/restore fix [S8-A] + CI release workflow wires sprint-6/7/8 [S8-B] + release.sh --prerelease workflow [S8-C] + sprint-8.sh harness self-audit [S8-Z]. Includes a runtime fixture test that seeds two fake tarballs and verifies both survive a [S7-C] run, and four runtime dry-run probes for the --prerelease mode × version quadrants.
  - `bash tests/integration/sprint-9.sh` — 39 assertions: SemVer-aware tarball selection in sprint-4.sh [S9-A] + package-plugin.sh gtar fallback for cross-host reproducibility [S9-B] + release.sh branch guard (stable cuts off main refused) [S9-C] + sprint-9.sh harness self-audit [S9-Z]. Runtime opt-outs: `VF_SKIP_S9A_RUNTIME=1` / `VF_SKIP_S9B_RUNTIME=1` / `VF_SKIP_S9C_RUNTIME=1`.
  - `bash tests/integration/sprint-10.sh` — 21 assertions: release.sh --notes-file pre-fill (static + 4-quadrant runtime) [S10-D] + sprint-10.sh harness self-audit [S10-Z]. [S10-A] PgBouncer probe + [S10-C] scheduled pg-matrix workflow retired in Sprint 11-E along with the Postgres backend. Runtime opt-out: `VF_SKIP_S10D_RUNTIME=1`.
  - `bash tests/integration/sprint-11.sh` — 20 assertions: bin/migrate-state-db-to-fs.sh round-trip (sqlite→project.json, --dry-run preserves legacy, second run no-op) [S11-F1] + bin/replay-events.sh rebuilds project.json byte-for-byte from events+archive [S11-F2] + sprint-11.sh harness self-audit [S11-Z].
  - `bash tests/integration/sprint-13.sh` — 55 assertions: hooks/scripts/phase-policy.json structural sanity [S13-A] + phase-write-guard.sh surface + hooks.json wire [S13-B]+[S13-C] + init skill regression sentinel (no codebase-explorer, no legacy mode prompt, Phase Contract present) + four Phase Contracts on critical skills + phase-policy.json skill registry coverage [S13-D] + PHASE-BOUNDARIES.md coverage for every phase and the escape hatch [S13-E] + retrospective-doc presence pins (SPRINT-11/12/13) [S13-F] + harness self-audit [S13-Z].
  - `bash tests/integration/sprint-14.sh` — 36 assertions: consensus-aggregator uses consensus.quorum override + detects codex/gemini CLIs + no longer reads vf_mode [S14-A] + aggregator filters non-reviewer subagents (reviewer=unknown bug) [S14-B] + mode concept fully retired (vf_mode gone, every hook + every committed config mode-free) [S14-C] + skill output contract — Write + explicit .vibeflow/reports/ path [S14-D] + harness self-audit [S14-Z].
  - `bash tests/integration/sprint-15.sh` — 82 assertions: consensus-orchestrator CLI output contract closes the codex/gemini plumbing gap [S15-A] + 6 analysis skills auto-invoke consensus via Final Step prose [S15-B] + phases.ts gains 5 new `consensus.<phase>.approved` exit criteria + validator scope-checks lastConsensusPhase [S15-C] + load-sdlc-context drains review-pending marker on SessionStart [S15-D] + claude-reviewer schema pins suggestions[] fields [S15-E] + new consensus-arbiter skill emits diff-first patches [S15-F] + new apply-arbiter-patch skill applies them with --yes/--run-tests/--dry-run [S15-G] + CONSENSUS-FLOW.md + ARBITER.md user docs [S15-H] + release sentinels [S15-I] + harness self-audit [S15-Z].
  - `bash tests/integration/sprint-16.sh` — 77 assertions: new `consensus-gate.sh` PreToolUse hook + hooks.json wiring [S16-A] + orchestrator/arbiter/apply drain `consensus-needed.json` marker [S16-B] + 6 analysis skills Write the marker in their Final Step with `{artifact, requiredCommand, createdAt, createdBy}` + S15-B `Final Step: Auto-Consensus` header preserved [S16-C] + docs/CONSENSUS-FLOW.md gains a "Layer 2 in detail" section [S16-D] + harness self-audit [S16-Z].
  - `bash tests/integration/sprint-17.sh` — 98 assertions: iterative consensus rounds (aggregator + orchestrator Step 5 with maxIterations/approvalThreshold/iteration.enabled) [S17-A] + critical finding dedup by {file,line_range} + Jaccard(title) + rejected-majority replacement rule [S17-B] + HUMAN_APPROVAL_REQUIRED enum + validator pass-through + humanOverrideNote threading + load-sdlc-context override advisory [S17-C] + 6 phase-specialist agents (prd-rewriter, design-spec-refiner, adr-author, test-strategy-refiner, coverage-gap-filler, runbook-editor) with diff-first + phase-contract sentinels [S17-D] + consensus-specialist dispatcher skill + phase-to-agent map + marker drain [S17-E] + ConsensusConfigSchema defaults + phase-policy registration [S17-F] + docs/CONSENSUS-ITERATION.md + docs/SPECIALISTS.md [S17-G] + harness self-audit [S17-Z].
  - `bash tests/integration/sprint-18.sh` — 67 assertions: architecture-validator auto-satisfies adr.recorded [S18-A] + test-strategy-planner auto-satisfies test-strategy.approved [S18-B] + coverage-analyzer auto-satisfies coverage.met [S18-C] + mutation-test-runner adds Final Step + auto-satisfies mutation.score.acceptable [S18-D] + consensus-orchestrator auto-satisfies code.reviewed on DEVELOPMENT + APPROVED/HUMAN_APPROVAL_REQUIRED [S18-E] + new quality-gates skill runs lint/typecheck/tests + auto-satisfies quality.gates.passed [S18-F] + new deploy-verifier skill cross-checks CI pipeline + observability + auto-satisfies deployment.verified + health.checks.passed [S18-G] + docs/AUTO-SATISFY.md per-criterion matrix + phase-policy registration [S18-H] + harness self-audit [S18-Z].
  - `bash tests/integration/sprint-19.sh` — 87 assertions: marker schema v2 (primaryArtifact + evidence[]) across 8 analysis skills + quality-gates marker-less sentinel [S19-A] + consensus-orchestrator primary-artifact resolution + PRIMARY/EVIDENCE review prompt + excerpt for >30k-token primaries [S19-B] + aggregator REJECTED rule requires rejected≥1 (runtime regression: 2 NEEDS_REVISION + 1 APPROVED + critical=2 + rejected=0 → NEEDS_REVISION) [S19-C] + consensus-specialist 4-case guardrail (APPROVED/genuine REJECTED/soft/NEEDS_REVISION+HUMAN_APPROVAL_REQUIRED) [S19-D] + apply-arbiter-patch Step 9 auto-chain + --no-chain + VF_SKIP_AUTO_CHAIN + convergence-stall guard + verdict.round iteration [S19-E] + new phase-runner skill + phase→analyzer map + mcp__sdlc-engine__sdlc_advance_phase direct call [S19-F] + PhaseRunnerConfigSchema defaults + phase-policy registration [S19-G] + docs/PRIMARY-ARTIFACT.md [S19-H] + harness self-audit [S19-Z].
  - `bash tests/integration/sprint-20.sh` — 85 assertions: durable consensus history.jsonl (aggregator verdict row + apply-arbiter arbiter-decision row + orchestrator primary.txt sidecar + idempotency + runtime probe) [S20-A] + learning-loop-engine consensus-history mode (four-modes, history.jsonl input, 6 detectors, pattern-detection catalog v2) [S20-B] + arbiter-decision effectiveness trace (applied→next-round delta, ineffective-theme) [S20-C] + reviewer-memory store (per-artifact cap, top-3 criticals, enabled kill switch) [S20-D] + orchestrator memory injection (build_memory_block, PRIOR REVIEW MEMORY, codex/gemini/claude-reviewer, tokenBudget, omit-on-empty) [S20-E] + consensus-gate primaryArtifact [S20-F] + decision-recommender consumes consensus-learning-report.md + gate-adjustment detection [S20-G] + LearningLoop/ReviewerMemory config schemas + phase-policy [S20-H] + docs/LEARNING-LOOP.md + docs/REVIEWER-MEMORY.md + CONSENSUS-FLOW/ITERATION refreshes [S20-I] + harness self-audit [S20-Z].
  - `bash tests/integration/sprint-21.sh` — 65 assertions: semantic primary-artifact excerption (excerpt_if_large helper + semantic/headtail dispatch + section-scored prose + first-section-always + head/tail fallback + references/excerption.md scoring terms) [S21-A] + reviewer-memory theme-aware compaction (summary fold + seenCount + Jaccard match + orchestrator surfaces recurring first + runtime probe) [S21-B] + phase-runner progress ledger (phase-runner-progress.json + vf_progress helper + step boundaries + terminal status + fresh-overwrite + ledger jq runtime probe) [S21-C] + /vibeflow:flow-status renders ledger + stale-guard [S21-D] + config excerptTokenBudget/excerptStrategy/compaction defaults [S21-E] + docs/EXCERPTION.md + docs/PROGRESS-LEDGER.md + REVIEWER-MEMORY/CONSENSUS-FLOW refreshes [S21-F] + harness self-audit [S21-Z].
  - `bash tests/integration/sprint-22.sh` — 56 assertions: learning-apply skill (3-lane classify config-tune/template-route/escalate + diff-first patch under .vibeflow/state/patches/learning-* + never-writes-config + manifest + decisions report) [S22-A] + apply confirm + schema re-validation gate (in-range passes / out-of-range + illegal-enum rejected via node EngineConfigSchema runtime probe) + maxRelativeStep clamp + minObservations floor [S22-B] + template-route phase→specialist map (prd-rewriter/adr-author/…) + no-auto-fork guardrail [S22-C] + LearningApplyConfigSchema defaults + phase-policy registration [S22-D] + docs/ACTION-GAP.md + docs/LEARNING-APPLY.md + LEARNING-LOOP/CONSENSUS-FLOW refreshes [S22-E] + harness self-audit [S22-Z].
  - `bash tests/integration/sprint-23.sh` — 43 assertions: AutoApplyConfigSchema default-OFF + nested partial-merge + regressionDelta range (config sentinels + node runtime probe) [S23-A] + learning-apply Step 7 bounded auto-apply (autoApply.enabled gate + autoApply.keys allowlist + snapshot-before-apply + watch.json arm + Applied:auto + re-validation still gates + cooldown skip + default-OFF) [S23-B] + aggregator revert-watch (same phase+artifact guard + agreement-vs-baseline delta + snapshot restore + cooldown + history auto-revert row + HELD log + watch drop; runtime restore/hold/no-watch carried by hooks/tests) [S23-C] + docs/AUTO-APPLY.md (default-off + allowlist + auto-revert + 5 guardrails) + LEARNING-APPLY/ACTION-GAP refreshes [S23-D] + harness self-audit [S23-Z].
  - `bash tests/integration/sprint-24.sh` — 41 assertions: durable auto-apply outcome telemetry (learning-apply type:auto-apply row + aggregator type:auto-apply-held row, both keyed; hook-test runtime) [S24-A] + learning-loop auto-tune-ineffective detector (revert-rate signature + ≥3 floor + remove-from-allowlist remediation + catalog v3 / 7 consensus-history patterns) [S24-B] + learning-apply self-demote (counts auto-revert rows ≥ demoteAfterReverts → demote allowlisted key to propose-only; config default 3) [S24-C] + multi-key transactional revert (one snapshot + watch.keys[] + aggregator keys // [key] + cooldown-all + atomic restore; config transactional default true; hook-test 2-key runtime) [S24-D] + docs/META-LEARNING.md + AUTO-APPLY/LEARNING-LOOP refreshes [S24-E] + harness self-audit [S24-Z].
  - `bash tests/integration/sprint-25.sh` — 42 assertions: read-only loop-audit.sh reader (consolidates history.jsonl auto-* outcomes + cooldown markers + watch.json + learning report + decisions into one guarded JSON summary; per-key revertRate + cooledDown + armedWatch + findingCount; empty-project degrade; runtime probe) [S25-A] + /vibeflow:flow-status Autonomous Loop section (runs loop-audit.sh, flags revertRate>=0.5, renders cooldowns/armed-watch/finding-count/recent-decisions, idle collapse) [S25-B] + LoopAuditConfigSchema window default 20 + range + EngineConfig wiring + node runtime probe [S25-C] + docs/LOOP-AUDIT.md + AUTO-APPLY/META-LEARNING refreshes [S25-D] + harness self-audit [S25-Z].
  - `bash tests/integration/sprint-26.sh` — 54 assertions: release-decision-engine auto-satisfies release.decision.go on GO only [S26-A] + opt-in entry-gate (GatesConfigSchema default-off + validator target-entryCriteria + engine/tools/server wiring + node runtime probe off→advances/on→blocked/on+GO→advances) [S26-B] + TESTING/DEPLOYMENT pipeline contract (coverage/mutation artifacts + auto-satisfy + phase-runner map + phases.ts criteria pins) [S26-C] + docs/TESTING-READINESS.md + AUTO-SATISFY/PHASE-BOUNDARIES refreshes [S26-D] + portable vf_run_timeout (timeout→gtimeout→pure-bash watchdog returns 124 + preserves piped stdin; no bare timeout 90 left; runtime EOF-safe/hang-124/error-rc probe) [S26-F] + codex pipe-form footgun fix (no argument form, warning) [S26-G] + harness self-audit [S26-Z].
  - `bash tests/integration/sprint-27.sh` — 44 assertions: autoForkSpecialist config opt-in (default-off + accepts-true via node runtime probe) [S27-A] + learning-apply auto-fork lane (off→recommend-only / on→learning-fork brief + fork specialist + patch under .vibeflow/state/patches/learning-fork-* + NEVER applies / apply-arbiter-patch confirm + minObservations/enabled guards) [S27-B] + 6 phase-specialist agents accept the learning-fork brief + agents/_shared/learning-fork-brief.md format (diff-first/never-write-source preserved) [S27-C] + docs/TEMPLATE-AUTONOMY.md + ACTION-GAP/LEARNING-APPLY refreshes [S27-D] + harness self-audit [S27-Z].
  - `bash tests/integration/sprint-28.sh` — 44 assertions: GlobalLearningConfigSchema default-off + vf_global_dir (VIBEFLOW_GLOBAL_DIR override) + vf_project_hash (one-way sha256) + node/helper runtime probes [S28-A] + privacy-safe global-store writes (aggregator reverted/held + learning-apply applied → {key,outcome,phase,projectHash} only, NO file names; gated on globalLearning.enabled; hook runtime) [S28-B] + cross-project-signal detector (hold-rate across ≥3 distinct projects, read-only never-auto-allowlist, catalog v4 / 8 consensus-history patterns, jq aggregate runtime probe) [S28-C] + docs/CROSS-PROJECT-LEARNING.md (opt-in/privacy/read-only/wipe/VIBEFLOW_GLOBAL_DIR) + META-LEARNING refresh [S28-D] + harness self-audit [S28-Z].
  - `bash tests/integration/sprint-29.sh` — 34 assertions: phase-runner TESTING generate-artifacts pre-step (Step 2a TESTING-only + tech.coverageCommand resolution + per-runner defaults [vitest/jest/pytest/dotnet] + generate:coverage ledger step + runner allowed-tools + command-resolution runtime probe) [S29-A] + best-effort continue-on-failure + --no-generate/VF_SKIP_GENERATE opt-out + TESTING-only guard [S29-B] + docs/TESTING-READINESS.md one-command-walk refresh [S29-C] + harness self-audit [S29-Z]. Skill-only sprint (no engine/hook change).
  - `bash tests/integration/sprint-30.sh` — 58 assertions: loop-audit.sh `phaseRunner` (from the Sprint-21 progress ledger) + `crossProject` (globalLearning-gated, from the Sprint-28 global store) sections — sentinels + a runtime probe seeding a ledger + a global store (status/step-counts/attempt + distinctProjects/holdRate populate; fresh→null; store-present-but-globalLearning-off→null; existing fields intact) [S30-A] + `/vibeflow:loop-status` skill (frontmatter + runs loop-audit.sh + renders phase-runner/auto-apply/cooldowns/armed-watch/learning/cross-project + revertRate≥0.5 flag + recommendation-only + writes .vibeflow/reports/loop-status.md + idle collapse + read-only + phase-policy ALL registration) [S30-B] + docs/LOOP-STATUS.md (new) + docs/LOOP-AUDIT.md refresh [S30-C] + harness self-audit [S30-Z]. Skill + hook only (no engine change).
  - `bash tests/integration/sprint-31.sh` — 41 assertions: init→onboard rename completeness (skills/onboard exists + skills/init gone + name/disable-model-invocation/Phase-Contract preserved + phase-policy repointed + no live `vibeflow:init` refs in skills/hooks + load-sdlc-context hints onboard) [S31-A] + `▶ Next:` breadcrumb convention on onboard→prd-quality-analyzer→phase-runner [S31-B] + `consensus-run.sh` (codex/gemini + `--finalize` + marker drain + exit-3 no-CLI) + `consensus-aggregator.sh --finalize` additive mode (FINALIZE_MODE guard + quorum-wait skip) + `consensus-gate.sh` allowlist + phase-runner wiring (calls runner + records consensus via MCP + no longer claims to fork the orchestrator) + orchestrator interactive-vs-headless note + a runtime probe (seed 2 reviewer lines → `--finalize` writes APPROVED verdict.json/total=2/history row with no quorum stall; SubagentStop path still logs a reviewer) [S31-C] + harness self-audit [S31-Z].
  - `bash tests/integration/sprint-32.sh` — 19 assertions: status→flow-status rename completeness (skills/flow-status exists + skills/status gone + name + phase-policy repointed + no functional vibeflow:status/skills/status refs in skills/hooks + loop-status kept) [S32-A] + zero-exact-collision audit (every skill name vs the Claude Code built-in set + retired status/init have no dir) [S32-B] + docs/COMMAND-COLLISIONS.md + CLAUDE.md flow-status key-command + stale /vibeflow:review removed [S32-C] + harness self-audit [S32-Z].
  - `bash tests/integration/sprint-34.sh` — 31 assertions: design-bootstrap skill structure (DESIGN Phase Contract + model-invocable + 3-source offer [Claude-native / existing-Figma via design-bridge / from-scratch via official Figma MCP] + always-ask + design-bridge tools in allowed-tools + output contract design/design-spec.md primary + tokens + consensus marker + ▶ Next breadcrumb + a11y section + FIGMA_TOKEN setup guidance) [S34-A] + phase-policy DESIGN registration + phase-runner DESIGN wiring [S34-B] + docs/DESIGN-PHASE.md (both Figma connection guides) [S34-C] + harness self-audit [S34-Z].
  - `bash tests/integration/sprint-35.sh` — 25 assertions: design-bootstrap Step 1b UI-facing classification (UI vs backend signals + backend-increment flag + ARCHITECTURE handoff note + still-asks) [S35-A] + option 4 Both/compare (3·D route + design/candidates + design-comparison.md + operator-pick → canonical design-spec.md + db_compare_impl + cp allowed-tool) [S35-B] + option 5 Technical design (3·E route + TECHNICAL spec + ARCHITECTURE recommendation + no-UI-no-tokens) + docs [S35-C] + harness self-audit [S35-Z].
  - `bash tests/integration/sprint-36.sh` — 17 assertions: design-bootstrap fair-fidelity rendering — compare-both Claude candidate emits self-contained HTML/CSS mockups (token-driven, under candidates/claude-native/mockups) so the Figma compare is rendered-vs-rendered not wireframe-vs-png + optional .png screenshot [S36-A] + Claude-native path prefers a rendered HTML mockup per hero screen + the "what merge means" explainer [S36-B] + docs/DESIGN-PHASE.md rendered-vs-rendered [S36-C] + harness self-audit [S36-Z].
  - `bash tests/integration/sprint-37.sh` — 30 assertions: architecture-bootstrap skill (ARCHITECTURE Phase Contract + model-invocable + asks tech-baseline [defaults/specify/stack-agnostic] + authors docs/architecture.md + docs/adr/ADR-NNN + domain-policy-aware security [encryption/RLS/WORM/kill-switch] + consensus marker + ▶ Next + brownfield ci_dependency_graph) [S37-A] + phase-policy ARCHITECTURE registration + phase-runner ARCHITECTURE wiring + the **Skill-not-Agent** fix (Step 2 runs analyzers via the Skill tool, not a general/Explore agent) [S37-B] + docs/ARCHITECTURE-PHASE.md [S37-C] + harness self-audit [S37-Z].
  - `bash tests/integration/sprint-38.sh` — 11 assertions: no skill frontmatter carries `agent: Explore` (the read-only Explore agent excludes Write → 27 Write-needing skills couldn't execute, returned codebase exploration; removed everywhere) [S38-A] + the Write-needing validators (architecture-validator/prd-quality-analyzer/test-strategy-planner/traceability-engine/coverage-analyzer/release-decision-engine) keep Write + no Explore routing → can execute [S38-B] + harness self-audit [S38-Z].
  - `bash tests/integration/sprint-39.sh` — 32 assertions: brownfield-intake skill (REQUIREMENTS Phase Contract + model-invocable + fingerprints via codebase-intel [ci_analyze_structure/ci_dependency_graph/ci_find_hotspots/ci_tech_debt_scan] + 4 intake paths [describe / doc / guided Q&A / gh-glab issue] + code-anchored increment-scoped requirements doc + change-type classification [UI/backend/infra] + phase implications + breadcrumb to prd-quality-analyzer) [S39-A] + phase-policy REQUIREMENTS registration + onboard greenfield/brownfield branch (brownfield → brownfield-intake, greenfield → prd-quality-analyzer) [S39-B] + docs/BROWNFIELD.md [S39-C] + harness self-audit [S39-Z].
  - `bash tests/integration/sprint-40.sh` — 16 assertions: apply-arbiter-patch Step 3.5 archives every target file to `.vibeflow/archive/<path>.<UTC-ts>.bak` (timestamped, filesystem-safe, persistent version trail) **before** the first `git apply`, distinct from the session rollback snapshot which stays [S40-A] + description + Output document the archive [S40-B] + runtime probe (archive bash writes a timestamped content-preserving copy + rollback snapshot also written) [S40-C] + harness self-audit [S40-Z].
  - `bash tests/integration/sprint-41.sh` — 16 assertions: the 3 auto-satisfy analyzers (quality-gates / release-decision-engine / deploy-verifier) are model-invocable so phase-runner can run them [S41-A] + phase-runner DEVELOPMENT diff resolved robustly (git-repo guard + merge-base with default branch → whole-increment review + HEAD~1 fallback + empty-tree for the first commit; no hardcoded HEAD~1..HEAD that exits 128) [S41-B] + runtime base-resolver probe over single-commit/two-commit/non-git repos [S41-C] + harness self-audit [S41-Z].
  - `bash tests/integration/sprint-42.sh` — 28 assertions: project lifecycle (cycles/resume/increments) — onboard Step 0 resume-guard (already-onboarded → resume/new-increment from `.vibeflow/state/lifecycle.json`, NOT re-onboard / NOT brownfield-by-file-presence) + opens cycle-1 (kind=initial) [S42-A] + phase-runner Step 0 lifecycle read (in-progress→resume / completed→new increment on fresh branch) + DEPLOYMENT closes the cycle on GO (not CONDITIONAL/BLOCKED) + one cycle=one branch/PR track-and-breadcrumb [S42-B] + brownfield-intake opens an increment cycle [S42-C] + docs/LIFECYCLE.md [S42-D] + runtime routing probe (no-lifecycle→ONBOARD / in-progress→RESUME / completed→NEW-INCREMENT) [S42-E] + harness self-audit [S42-Z].
  - `bash tests/integration/sprint-43.sh` — 13 assertions: coverage-analyzer [S43-A] + mutation-test-runner [S43-B] arm the consensus marker **only on VERDICT==PASS** (non-PASS → advisory + ▶ Next fix breadcrumb, no marker, so consensus-gate doesn't block the fix iteration; marker JSON schema preserved on the PASS path) + harness self-audit [S43-Z].
  - `bash tests/integration/sprint-44.sh` — 34 assertions: input-validation-matrix skill (TESTING Phase Contract + model-invocable + UI-only guard + field inventory from design/requirements/source + all categories [required/type/boundary/format/injection-safety/output-control/cross-field] + writes tests into the suite + validation-matrix.md + arm-on-pass) [S44-A] + phase-runner TESTING UI-conditional front-end battery (input-validation-matrix + e2e-test-writer + business-rule-validator + traceability-engine + visual-ai-analyzer + uat-executor; UI-facing only, backend skips; Skills-not-agents; gated via suite) [S44-B] + phase-policy registration + docs/FRONTEND-TESTING.md (concern→skill map + gate-not-side-report + A/B out-of-scope) [S44-C] + harness self-audit [S44-Z].
  - `bash tests/integration/sprint-45.sh` — 32 assertions: mobile-stability-runner skill (TESTING mobile-only Phase Contract + model-invocable + Expo-aware runner auto-detect [Expo→Maestro / bare→Detox] + crash-focused battery [cold-start/background/low-memory/deep-links/permission/network-loss/rotation] + native-crash/redbox/ANR detection + crash-reporter [Sentry/Crashlytics/Expo] surfacing + graceful-degrade when no device + mobile-stability-report.md + arm-on-pass) [S45-A] + phase-runner TESTING mobile-conditional wiring (platform ios/android/all) [S45-B] + phase-policy registration + docs/MOBILE-TESTING.md [S45-C] + harness self-audit [S45-Z].
  - `bash tests/integration/sprint-46.sh` — 17 assertions: cycle-completion clarity — phase-runner Step 0 + DEPLOYMENT-close note + onboard Step 0 + docs/LIFECYCLE.md state that a merged PR / reaching DEPLOYMENT is NOT completion (only DEPLOYMENT GO flips status:completed), an in-progress cycle (even at DEPLOYMENT) resumes + never gets a new-increment/brownfield breadcrumb [S46-A] + stuck-DEPLOYMENT (no CI/deploy infra) guidance: finish-deploy OR operator-closes/defers, never auto-new-increment [S46-B] + onboard/docs completion clarity [S46-C] + harness self-audit [S46-Z].
  - `bash tests/integration/sprint-47.sh` — 19 assertions: consensus-run.sh + orchestrator capture reviewer CLIs **stdout-only** (no `2>&1`; codex `exec` echoes the prompt's JSON schema template `{verdict:APPROVED|NEEDS_REVISION|REJECTED,…}` to stderr → merging it poisoned the verdict blob → content-free REJECTED flipped the aggregate; stderr now → a separate file for warn_cli_error) [S47-A] + keyword-grep defense drops the `|`-alternation schema-template line before classifying (+ runtime: schema-template-only blob doesn't yield REJECTED) [S47-B] + orchestrator prose stdout-only (codex+gemini) [S47-C] + walk-all-objects extractor kept + harness self-audit [S47-Z].
  - `bash tests/integration/sprint-48.sh` — 22 assertions: sdlc_start_cycle engine reset for the Sprint-42 multi-cycle lifecycle — tools.ts registers the tool + StartCycleInput + engine.startCycle (resets currentPhase→REQUIREMENTS + clears criteria/consensus + bumps ProjectState.cycle) + unit test + live engine build/201-test/dist check [S48-A] + brownfield-intake calls it on a new increment + phase-runner Step 0 reconciles engine/lifecycle drift (both allowed-tools) [S48-B] + docs/LIFECYCLE.md two-stores section [S48-C] + harness self-audit [S48-Z].
  - `bash tests/integration/sprint-49.sh` — 11 assertions: engine normalizes HTML entities in a criterion name (`normalizeCriterion` helper decodes &gt;/&lt;/&amp;-last/quotes + trim; applied in satisfyCriterion on the single write path; stores the canonical literal so an escaped `testability.score&gt;=60` matches the literal `testability.score>=60` exit gate) + unit test + live engine build/202-test + a live node probe of the exported helper [S49-A] + harness self-audit [S49-Z].
  - `bash tests/integration/sprint-50.sh` — 11 assertions: consensus-gate honors `VF_SKIP_CONSENSUS_GATE=1` even as a command-prefix env var alongside another (Claude Code reliably exports a single leading assignment but not two → `VF_SKIP_CONSENSUS_GATE=1 VF_ALLOW_PHASE_WRITE=1 bash` was wrongly blocked) — gate now parses the **leading env-var prefix** of the command string (awk stops at the first non-assignment token) so the bypass is order/count-independent; env check kept [S50-A] + runtime probes (single/double/reversed bypass allow; non-bypass + quoted-mention still block) [S50-B] + harness self-audit [S50-Z].
  - `bash tests/integration/sprint-51.sh` — 15 assertions: phase-write-guard source patterns are monorepo-aware — TESTING + DEPLOYMENT `warn` lists add `**/src/**` alongside `src/**` (was only top-level, so monorepo `packages/*/src/…` fell to hard-block instead of the intended warn — bit a legitimate Stryker equivalent-mutant annotation in TESTING) [S51-A] + runtime matcher probe (monorepo `packages/*/src` + `apps/*/src` + top-level src → warn; monorepo test file → allow; non-src `lib/` → block not over-permissive; REQUIREMENTS source → block no regression) [S51-B] + docs/PHASE-BOUNDARIES.md monorepo-aware note [S51-C] + harness self-audit [S51-Z].
  - `bash tests/integration/sprint-52.sh` — 11 assertions: phase-runner Step 0 surfaces in-flight work before progressing — checks `git status --porcelain` (uncommitted) + `review-pending.json` and names them with a heads-up + git add/commit breadcrumb (uncommitted work isn't in the DEVELOPMENT code.reviewed diff); surface-only (never auto-commits / never blocks / quiet when nothing pending); allowed-tools add `git status`/`git rev-parse` read-only + a guard that no git add/commit/push is allowed [S52-A] + harness self-audit [S52-Z].
  - `bash tests/integration/sprint-53.sh` — 29 assertions: frontend-render-check (TESTING web-only + boots dev server [won't-boot/path-with-spaces = finding] + framework detect + mock data no-backend + captures **+ surfaces** hero screenshots [anti-dark-tunnel + localhost URL] + implemented-vs-designed compare [db_compare_impl / design-spec mockups+tokens, side-by-side] + BLOCKED on won't-boot/no-relation + frontend-conformance.md + arm-on-pass + graceful-degrade + live-front+back-is-uat note) [S53-A] + phase-runner TESTING battery first lane + uat live-front+back note [S53-B] + phase-policy registration + docs/FRONTEND-TESTING.md dark-tunnel + mock-now/live-later [S53-C] + harness self-audit [S53-Z].
  - `bash tests/integration/sprint-54.sh` — 20 assertions: `ui-verification-debt.sh` read-only reader (detects a web UI [framework dep + web entry] with no `frontend-conformance.md` → `never` / report older than UI source → `stale` / else `verified` / no web UI → `no-ui`; fail-safe; surface-only) + runtime probes (no-ui/never/verified/stale/backend-only) [S54-A] + phase-runner Step 0 + flow-status surface it (⚠ never/stale → frontend-render-check; verified/no-ui → silent) + allowed-tools [S54-B] + harness self-audit [S54-Z].
  - `bash tests/integration/sprint-55.sh` — 20 assertions: `ui-styling-check.sh` read-only detector classifies a web UI as `skinless` (className used but no stylesheet/Tailwind/CSS-in-JS/token consumption — design specified but never implemented), `styled`, or `no-ui` + runtime probes (no-ui / skinless[ANTOS case] / styled[stylesheet/Tailwind/token] / backend-only) [S55-A] + phase-runner DEVELOPMENT surfaces skinless prominently (absence invisible to quality-gates/code.reviewed) + frontend-render-check static skinless pre-check → hard BLOCKED + allowed-tools [S55-B] + harness self-audit [S55-Z].
  - `bash tests/integration/sprint-56.sh` — 19 assertions: phase-runner setup no longer trips consensus-gate — gate allowlists VibeFlow read-only readers (ui-verification-debt/ui-styling-check/loop-audit) + `git status`/`git rev-parse` (inspect-only, don't bypass consensus) while a **write** (`> src/foo.ts`) or forward work (`npm run build`) still BLOCKs + runtime probes [S56-A] + phase-runner Step 0 reads lifecycle via the **Read tool** + phase via **MCP** (ungated), not Bash, and doesn't reach for VF_SKIP just to read state [S56-B] + harness self-audit [S56-Z].
  - `bash tests/integration/sprint-57.sh` — 31 assertions: `test-strategy-planner` Step 2b UI scenario enumeration (UI-conditional: platform web/all + `design/design-spec.md` via Glob; skipped+recorded otherwise) enumerates per hero-screen/field **visual-conformance** (`SCN-UI-<screen>-V-NN` + default/hover/focus/disabled/error/loading states) + **field-validation** (`SCN-UI-<screen>-VAL-NN` required/type/boundary/format) + **backend-data-binding** (`SCN-UI-<screen>-DATA-NN` correct server value + loading/populated/empty/error/offline) [S57-A] + scenario-set.md `## UI Scenarios` block + metadata UI count + RTM carries the `FR-* → screen → UI scenario` chain + every UI scenario traced back to its requirement [S57-B] + downstream front-end battery named (frontend-render-check/input-validation-matrix/visual-ai-analyzer/uat-executor) + coverage-gap UI categories (UI_UNTESTED/UI_DATA_GAP/DESIGN_REQ_GAP, never invent a requirement) + allowed-tools Glob [S57-C] + harness self-audit [S57-Z].
  - `bash tests/integration/sprint-58.sh` — 47 assertions: `integration-wiring-check.sh` read-only detector classifies the front↔back boundary `wired`/`unwired`/`single-tier`/`no-app` (front-end vs back-end source dirs split by nearest package.json so an axios `.get(` in the UI isn't read as a server route) + runtime probes (no-app / backend-only→single-tier / static-fe→single-tier / UI-fetch+no-backend→unwired [the ANTOS case] / UI-fetch+backend-no-routes→unwired / UI-fetch+routes→wired / axios-in-FE guard) [S58-A] + `integration-verifier` skill (TESTING full-stack-only Phase Contract + model-invocable + gates on the detector [single-tier/no-app skip] + boots the real back-end **locally** [no staging; no-HTTP-server is itself BLOCKED] + seeds deterministic data via test-data-manager + points the real FE at the local BE off mocks + drives `SCN-UI-*-DATA-*` data-binding scenarios [populated/loading/empty/error/offline] + `curl` 404 check + arm-on-pass) [S58-B] + phase-runner TESTING wiring (integration-verifier between frontend-render-check and uat-executor, gated wired/unwired, three-rung ladder reframe [mock-render→local-integration→deployed-staging] + names the missing-middle-rung problem + allowed-tools + registry registration) [S58-C] + phase-runner DEVELOPMENT surfaces an `unwired` UI prominently [caught while built, hard gate stays in TESTING] [S58-D] + docs (`docs/INTEGRATION-TESTING.md` new + `docs/FRONTEND-TESTING.md` three-rung refresh + frontend-render-check note repointed) [S58-E] + harness self-audit [S58-Z].
  - `bash tests/integration/sprint-59.sh` — 33 assertions: `integration-wiring-check.sh` is now **stack-agnostic** — detects the back-end **manifest-first** across Node (`package.json`)/Python (`pyproject.toml`/`requirements.txt`)/Go (`go.mod`)/.NET (`*.csproj`/`*.sln`)/Java (`pom.xml`/`build.gradle`) with per-language route patterns scoped to each stack's dirs, front-end framework-optional (plain `index.html`+`fetch` counts; static mockup stays single-tier) + runtime probes (JS regression [empty→no-app / express routes→wired / axios-in-FE guard→unwired] + Python FastAPI routes+UI→wired / Python no-routes+UI→unwired / Python backend-only→single-tier [the Clera shape] / Go routes+UI→wired / .NET MapGet+UI→wired / framework-less index.html+fetch+Python→wired) [S59-A] + `integration-verifier` boots any stack (Step 1 manifest-first + Python `uvicorn`/Go `go run`/.NET `dotnet run` + `VF_BACKEND_CMD`/`VF_BACKEND_PORT` override + no-HTTP-server library [Clera shape] → BLOCKED + allowed-tools python/uvicorn/gunicorn/go/dotnet/mvn/gradle) [S59-B] + phase-runner stack-agnostic note [S59-C] + docs/INTEGRATION-TESTING.md Supported-stacks matrix + override + server-rendered note [S59-D] + harness self-audit [S59-Z].
  - `bash tests/integration/sprint-60.sh` — 63 assertions: **parallel team work via per-branch work-streams** (opt-in `streams.enabled`, default OFF). `StreamsConfigSchema` (config.ts, default-off + `idStrategy` branch|fixed) + `vf_stream_id`/`vf_branch_slug`/`vf_lifecycle_path` (`_lib.sh`) derive a per-git-branch `projectId` (`<project>__<slug>`) so each branch/worktree gets its own isolated `.vibeflow/state/<id>/` (the engine already locks+isolates per projectId — no engine logic change); default branch/detached/non-git collapse to the bare project id (existing single-stream projects bit-for-bit unchanged) [S60-A] + new read-only `stream-id.sh` (resolve stream id + lifecycle path) and `streams-audit.sh` (list local streams) readers + `consensus-gate` allowlists both + two-branch state-isolation runtime [S60-B] + new `/vibeflow:streams` dashboard + `/vibeflow:integrate` merge-point skill (opens an integration cycle via `sdlc_start_cycle` + runs `integration-verifier` over the combined surface + cross-feature consensus) + registry + onboard/phase-runner/brownfield-intake resolve the stream id + flow-status one-line glance [S60-C] + consensus-gate read-only-reader allowlist runtime [S60-D] + docs (`docs/TEAM-WORK.md` new + LIFECYCLE/CLAUDE refresh) [S60-E] + harness self-audit [S60-Z].
  - `bash tests/integration/sprint-61.sh` — 57 assertions: **operator decisions become tappable choices** (default-on, mobile/remote-first). `docs/OPERATOR-CHOICES.md` convention + CLAUDE.md pointer [S61-A] + nine operator-blocking skills (onboard, brownfield-intake, design-bootstrap, architecture-bootstrap, apply-arbiter-patch, phase-runner, integrate, release-decision-engine, learning-apply) surface 2–4 options via the **`AskUserQuestion`** tool (recommended first, condensed rationale per option; the built-in "Other" always preserves manual typing) instead of a prose menu / `[y/N]` the operator must read+type — `AskUserQuestion` added to each `allowed-tools`, the prose menu kept as fallback; no config flag (default-on is pure upside) [S61-B] + harness self-audit [S61-Z]. Pure skill-prose + frontmatter + docs (no engine/hook/MCP change).
  - `bash tests/integration/sprint-62.sh` — 16 assertions: **consensus-gate honors the bypass after a leading `cd <path> &&|;` navigation preamble** (the `cd /repo && VF_SKIP_CONSENSUS_GATE=1 cmd` shape — the awk env-prefix scan used to stop at `cd` and never see the override). `consensus-gate.sh` now strips one or more leading `cd … (&&|;)` segments into `GATE_SCAN` before the env-prefix scan; **only `cd`** is strippable (real work can't sneak ahead ungated) and a quoted mention after the cd still doesn't bypass [S62-A] (static sentinels + 8 runtime probes: cd;/cd&&/quoted-path/double-cd/leading-bypass → allow; cd&&quoted-mention / non-cd-work&&bypass / plain-write → block) + harness self-audit [S62-Z]. Hook-only change (consensus-gate.sh); no engine/skill change. + S65-A SessionStart ambient operator-choice convention line (load-sdlc-context.sh nudges AskUserQuestion for ad-hoc operator choices; budget raised 250→450) + S66-A vf_web_ui_kind stack-agnostic web-UI detector (js/server-rendered/static/none; FastAPI HTMLResponse + backend static/index.html + templates → server-rendered; design-mockups → none) + ui-verification-debt/ui-styling-check detect server-rendered (no longer no-ui)
  - `bash tests/integration/sprint-63.sh` — 30 assertions: **read-only status/dashboard views surface their closing "Next step" as a tappable `AskUserQuestion` choice** (not prose). `flow-status` / `loop-status` / `streams` each gain `AskUserQuestion` in `allowed-tools` + a next-step-as-choice instruction (recommended first, **always a "Stay / do nothing" option**, "Other" to type, card omitted when nothing actionable) — flow-status specifically covers advance / branch-increment-mismatch / render-debt / streams next-steps (the live FlowBridge_TR gap: flow-status emitted a prose "Next step: run /vibeflow:advance" instead of a tappable option because it wasn't in the Sprint-61 nine) [S63-A] + docs/OPERATOR-CHOICES.md documents the terminal-next-step rule + the three views [S63-B] + harness self-audit [S63-Z]. Skill-prose + docs only.
  - `bash tests/integration/sprint-64.sh` — 62 assertions: **deterministic single-step `▶ Next:` breadcrumbs are also tappable cards**. The 13 breadcrumb-emitting skills (architecture-bootstrap/brownfield-intake/coverage-analyzer/design-bootstrap/frontend-render-check/input-validation-matrix/integrate/integration-verifier/mobile-stability-runner/mutation-test-runner/onboard/phase-runner/prd-quality-analyzer) each gain `AskUserQuestion` in `allowed-tools` (7 added; 6 already had it from Sprint 61) + a uniform "Breadcrumb as a tappable card" block that presents the closing `▶ Next: <command>` as **Run `<command>` now (Recommended) / Not yet** (Other to type; literal breadcrumb kept as fallback; card skipped when the breadcrumb names no runnable command) — so the SDLC walks phone-first, one tap per handoff [S64-A] + docs/OPERATOR-CHOICES.md single-deterministic-next rule [S64-B] + **fixed 4 latent buggy `allowed-tools:[^\n]*` sentinels** (sprint-4/14/15/16) — the `[^\n]` idiom matches "not backslash-or-n", so an allowed-tools value containing a literal `n` before Write/Edit (e.g. `AskUserQuestion`) defeated the check; corrected to `.*` [S64-C] + harness self-audit [S64-Z]. Skill-prose + docs + test-sentinel fixes.
  - `bash tests/integration/sprint-65.sh` — 11 assertions: **ambient operator-choice convention on SessionStart**. Sprint 61/63/64 made *documented skill decision points* tappable, but an **ad-hoc question the model poses while orchestrating** (outside any skill — e.g. "build the API layer first, or go straight to DEVELOPMENT? which one?") falls back to prose. `load-sdlc-context.sh` (SessionStart) now emits a one-line convention nudging the whole session to present **any** operator choice/confirmation via `AskUserQuestion` (tappable options + "Other"), not a prose "which one?" — best-effort session-wide hint, not a hard skill instruction; budget raised 250→450 (still bounded) [S65-A] (static + runtime probe: line present + "VibeFlow active" state line kept + ≤450 chars) + harness self-audit [S65-Z]. Hook + test-budget only.
  - `bash tests/integration/sprint-66.sh` — 26 assertions: **the visual battery is stack-agnostic**. New `vf_web_ui_kind` (`_lib.sh`) detects a web UI as `js` / `server-rendered` / `static` / `none` — so a **server-rendered** app (FastAPI/Flask `HTMLResponse`, Jinja/Django/Rails/Razor templates, a backend-served `static/index.html` — the Clera shape) is no longer read as `no-ui`; design-mockup/vendored trees excluded [S66-A] + `ui-verification-debt.sh` + `ui-styling-check.sh` use it (server-rendered → never/styled/skinless, not no-ui; pure backend still no-ui) [S66-B] + `frontend-render-check` Phase Contract stack-agnostic + new **Step 1-SR** boots the REAL server (`uvicorn`/`flask`/`django`/`dotnet`/`rails`, Sprint-59 boot + `VF_BACKEND_CMD`/`VF_BACKEND_PORT`; no-HTTP-server library → BLOCKED) + boot binaries in allowed-tools [S66-C] + docs/FRONTEND-TESTING.md server-rendered section [S66-D] + harness self-audit [S66-Z]. Hook + skill + docs (no engine change).
- Total baseline: **3514 passing checks** across **63 test layers** (v2.7.0: +87 sprint-19.sh + +3 sdlc-engine PhaseRunnerConfigSchema tests; Sprint 60 added sprint-60.sh [+63] + sdlc-engine StreamsConfigSchema [202→206] + hooks S60-A/S60-B [185→198]; Sprint 61 added sprint-61.sh [+57], pure skill-prose so no unit/hook delta) (sprint-8.sh carries 4 pre-existing `[S8-C]` prerelease runtime failures that are scoped to release-tooling edge cases; the 29 passing assertions from sprint-8 are included in the total. Pre-existing-on-clean-tree failures unrelated to a given sprint: sprint-8 [S8-C], sprint-11 [S11-F2 replay], sprint-20 [S20-B] + sprint-24 [S24-B] consensus-history catalog-count drift, sprint-47 [S47-B]). Sprint 4 ✅ COMPLETE + v1.0.0 shipped. Sprint 5 ✅ COMPLETE + v1.0.1 shipped. Sprint 6 ✅ COMPLETE + v1.1.0 shipped. Sprint 7 ✅ COMPLETE + v1.2.0 shipped 2026-04-16. Sprint 8 ✅ COMPLETE + v1.3.0 shipped 2026-04-17. Sprint 9 ✅ COMPLETE + v1.4.0 shipped 2026-04-17. Sprint 10 ✅ COMPLETE + v1.5.0 shipped 2026-04-19. Sprint 11 ✅ COMPLETE — filesystem-only state store (breaking change), v2.0.0 shipped 2026-04-20. sqlite + postgres backends + TEAM-MODE.md + pg-matrix workflow retired with the refactor. Sprint 12 ✅ COMPLETE — esbuild-bundled MCP servers + `github:` marketplace flow, v2.0.1 shipped 2026-04-20; v2.0.2 hotfix (plugin.json repository/bugs as string per loader schema) on same day. Sprint 13 ✅ COMPLETE — generic phase enforcement via `phase-write-guard.sh` PreToolUse hook, `hooks/scripts/phase-policy.json` path policy, skill Phase Contracts, and `docs/PHASE-BOUNDARIES.md` user reference. v2.1.0 shipped 2026-04-20. Sprint 14 ✅ COMPLETE — consensus flow is CLI-availability-driven (codex/gemini gate-free; no more solo/team mode gate), `reviewer=unknown` bug closed by filtering non-reviewer subagents, `mode` concept fully retired (vf_mode deleted, every hook + config + doc cleaned), and four skills' output contracts fixed so `.vibeflow/reports/*.md` artifacts actually land on disk. v2.2.0 shipped 2026-04-20. Sprint 15 ✅ COMPLETE — MyVibe closed-loop completed: consensus-orchestrator now appends codex/gemini CLI verdicts directly into the session jsonl (closes the 600s-timeout demotion bug), 6 analysis skills auto-invoke consensus, phase-scoped consensus exit criteria block `/vibeflow:advance` without a fresh per-phase APPROVED verdict, new `consensus-arbiter` skill turns reviewer suggestions into diff-first patches (never writes to source), new `apply-arbiter-patch` skill applies with preview + explicit confirmation. v2.3.0 shipped 2026-04-20. Sprint 16 ✅ COMPLETE — auto-consensus chain made deterministic: analysis skills now Write `.vibeflow/state/consensus-needed.json` as their Final Step, a new `consensus-gate.sh` PreToolUse hook blocks every subsequent `Bash|Write|Edit` tool call until the operator runs `/vibeflow:consensus-orchestrator`, and the orchestrator + arbiter + apply skills all drain the marker on success. Closes the "forked subagent can't invoke a slash command" gap in Layer 2. v2.4.0 shipped 2026-04-20. Sprint 17 ✅ COMPLETE — iterative consensus negotiation loop, critical finding dedup (by {file,line_range} or Jaccard title similarity), new HUMAN_APPROVAL_REQUIRED status (pass-with-audit) when `consensus.maxIterations` rounds pass without reaching `consensus.approvalThreshold`, six new phase-specialist subagents (one per SDLC phase) that deeply rewrite artifacts based on reviewer feedback as one unified diff-first patch, and a `consensus-specialist` dispatcher skill that forks the right specialist based on currentPhase. Retires the pre-17 `agreement<0.5 → REJECTED` rule that was falsely promoting unanimous NEEDS_REVISION to REJECTED. v2.5.0 shipped 2026-04-20. Sprint 17.1-.4 polish (v2.5.1/.2/.3/.4): ship `/vibeflow:advance` skill with default-to-next; fix `.mcp.json` paths to use `${CLAUDE_PLUGIN_ROOT}` so MCP tools surface in consumer projects; auto-record consensus + auto-satisfy prd.approved/testability.score>=60 from the orchestrator + prd-quality-analyzer. Sprint 18 ✅ COMPLETE — full auto-satisfy rollout: architecture-validator/test-strategy-planner/coverage-analyzer/mutation-test-runner auto-satisfy their respective exit criteria; orchestrator auto-satisfies `code.reviewed` on DEVELOPMENT APPROVED; new `quality-gates` skill runs lint/typecheck/tests and satisfies `quality.gates.passed`; new `deploy-verifier` skill cross-checks CI + endpoints + observability and satisfies `deployment.verified` + `health.checks.passed`. Remaining three criteria (`design.approved`, `accessibility.verified`, `sprint.planned`) kept operator-driven and documented in `docs/AUTO-SATISFY.md` with rationale. v2.6.0 shipped 2026-04-21. v2.6.1 hotfix (2026-04-22) added `save-plan-doc.sh` plan-persistence hook + backfilled SPRINT-14..18 docs. Sprint 19 ✅ COMPLETE — refocus consensus on primary artifacts (marker schema v2 with primaryArtifact + evidence[], orchestrator reviews the PRD not the analyzer's report); aggregator REJECTED rule requires explicit reject votes (eliminates false-positive rejects that blocked iteration); specialist 4-case guardrail softens on NEEDS_REVISION; apply-arbiter-patch Step 9 auto-chain triggers next consensus round on the updated primary with convergence-stall guard + maxIterations ceiling; new `phase-runner` skill walks the full analyzer → consensus → specialist/arbiter → apply → advance loop in one command. v2.7.0 shipped 2026-04-23. Sprint 20 ✅ COMPLETE — close the L3 learning loop: `consensus-aggregator.sh` appends a durable cross-session `history.jsonl` roll-up (verdict rows + apply-arbiter `arbiter-decision` rows, idempotent by {sessionId, round}); `learning-loop-engine` gains a fourth `consensus-history` mode that mines it for slow-converging phases, reviewer skew, recurring suggestion themes, convergence stalls, primary-artifact churn, and the arbiter-decision effectiveness trace (applied-theme → next-round agreement delta → `ineffective-theme`); cross-session reviewer memory (`reviewer-memory/<reviewer>.jsonl`, capped per reviewer+artifact) is injected as a `PRIOR REVIEW MEMORY` block into each reviewer's prompt so reviewers stop re-litigating resolved points; `consensus-gate.sh` names the primary artifact; `decision-recommender` consumes the learning report; new `LearningLoopConfigSchema` + `ReviewerMemoryConfigSchema`. Docs: `docs/LEARNING-LOOP.md` + `docs/REVIEWER-MEMORY.md`. v2.8.0 shipped 2026-05-31. Sprint 21 ✅ COMPLETE — signal-quality refinement (no new loop mechanics): semantic primary-artifact excerption (`consensus-orchestrator` scores doc sections by evidence-finding density + reviewer-memory hits + keyword overlap, always keeps the first section, falls back to positional head/tail; `consensus.excerptStrategy`/`excerptTokenBudget`); theme-aware reviewer-memory compaction (entries dropped past the cap fold into a per-artifact recurrence summary with `seenCount` via Jaccard match, orchestrator surfaces recurring criticals first; `reviewerMemory.compaction`); phase-runner progress ledger (`.vibeflow/state/phase-runner-progress.json` updated per step boundary + terminal status, rendered by `/vibeflow:flow-status` with a stale-guard). Docs: `docs/EXCERPTION.md` + `docs/PROGRESS-LEDGER.md`. v2.9.0 shipped 2026-05-31. Sprint 22 ✅ COMPLETE — close the action gap: new `learning-apply` skill turns `consensus-learning-report.md` recommendations into action via three lanes — config-tune (diff-first patch to `vibeflow.config.json` under `.vibeflow/state/patches/learning-<ts>/`, applied only with `--yes` after an `EngineConfigSchema` re-validation gate + `maxRelativeStep` clamp + `minObservations` floor), template-route (recurring-theme → `/vibeflow:consensus-specialist` dispatch recommendation via the phase→specialist map; does not auto-fork), escalate (→ `decision-recommender`). Propose-only — never writes config or source directly. New `LearningApplyConfigSchema` + phase-policy registration. Docs: `docs/ACTION-GAP.md` + `docs/LEARNING-APPLY.md`. v2.10.0 shipped 2026-06-02. Sprint 23 ✅ COMPLETE — bounded auto-apply with auto-revert (the slow loop can now act autonomously, safely): new `AutoApplyConfigSchema` (nested in `learningApply`, **OFF by default**) opts allowlisted config keys into auto-apply; `learning-apply` Step 7 snapshots `vibeflow.config.json`, `git apply`s an allowlisted tune with no prompt (`Applied: auto`) after the full Sprint 22 gate chain, and arms `.vibeflow/state/auto-apply/watch.json`; `consensus-aggregator.sh` evaluates the watch after finalising the next round and, if the same `{phase, primaryArtifact}` agreement dropped past `regressionDelta`, restores the snapshot + logs an `auto-revert` + arms a per-key cooldown (else "AUTO-APPLY HELD"); non-allowlisted keys + source/templates stay operator-confirmed. Docs: `docs/AUTO-APPLY.md`. v2.11.0 shipped 2026-06-04. Sprint 24 ✅ COMPLETE — self-correcting auto-apply (the loop learns from its own tuning): every auto-tune outcome is a per-key `history.jsonl` row (`auto-apply` from learning-apply, `auto-revert`/`auto-apply-held` from the aggregator); `learning-loop-engine` consensus-history mode gains an `auto-tune-ineffective` detector (revert-rate ≥ 0.5 over ≥ 3 attempts → recommend removing the key from `autoApply.keys`; catalog v3); `learning-apply` self-demotes a key with ≥ `autoApply.demoteAfterReverts` (default 3) lifetime reverts back to propose-only even while allowlisted; multi-key transactional revert (`autoApply.transactional`, default true) arms one snapshot + one `watch.keys[]` so a run's tunes revert/hold as an atomic set with every key cooled down (aggregator reads `keys // [key]` for Sprint-23 back-compat). Docs: `docs/META-LEARNING.md`. v2.12.0 shipped 2026-06-05. Sprint 25 ✅ COMPLETE — autonomous-loop observability: new read-only `hooks/scripts/loop-audit.sh` consolidates the scattered loop telemetry (`history.jsonl` auto-apply outcomes, `cooldown-*` markers, `auto-apply/watch.json`, `consensus-learning-report.md`, `learning-apply-decisions.md`) into one guarded JSON summary (per-key applied/reverted/held + revertRate + cooledDown, armed watch, finding count, recent decisions); `/vibeflow:flow-status` gains an **Autonomous Loop** section that runs the reader, flags any key at `revertRate ≥ 0.5` (the S24-B signal), and collapses to a single idle line when there's no activity; new `LoopAuditConfigSchema { window: 20 }` bounds the per-key tally to recent rows. Docs: `docs/LOOP-AUDIT.md`. v2.13.0 shipped 2026-06-05. Sprint 26 ✅ COMPLETE — TESTING→DEPLOYMENT consumer hardening (FlowBridge-driven): `release-decision-engine` auto-satisfies `release.decision.go` on a GO verdict (the release decision now flows into project state / `/vibeflow:flow-status` / loop-audit); new `GatesConfigSchema { enforceEntryCriteria: false }` opt-in entry-gate makes `/vibeflow:advance` also enforce the target phase's `entryCriteria` when on (threaded config→engine→tools→validator); `docs/TESTING-READINESS.md` consumer walkthrough (incl. "empty `.vibeflow/artifacts/` in DEVELOPMENT is normal"). Plus two FlowBridge-surfaced consensus-CLI bugs FIXED (bug tracker #14/#15): macOS ships no `timeout` so `consensus-orchestrator` recorded every CLI as cli_error → new portable `vf_run_timeout` (timeout→gtimeout→pure-bash watchdog returning 124 + preserving the piped prompt); and the documented `codex exec "$(cat …)"` argument form hung indefinitely (open stdin) → fixed to the pipe form (stdin EOF). v2.14.0 shipped 2026-06-05. Sprint 27 ✅ COMPLETE (backlog B1) — template-route autonomy, propose-only: new `learningApply.autoForkSpecialist` (default OFF); when on, a `recurring-suggestion-theme` learning finding makes `learning-apply` write a learning-fork brief to `.vibeflow/state/patches/learning-fork-<ts>/`, **auto-fork** the matching phase specialist (prd-rewriter/adr-author/…) → diff-first patch — but **never applies it** (operator confirms via `/vibeflow:apply-arbiter-patch`); the six phase-specialist agents now accept a learning-fork brief (`agents/_shared/learning-fork-brief.md`) as an equivalent trigger to a consensus session. Templates stay confirm-only by design (unlike config's bounded auto-apply — large/unbounded rewrites aren't mechanically revertible). Docs: `docs/TEMPLATE-AUTONOMY.md`. v2.15.0 shipped 2026-06-05. Sprint 28 ✅ COMPLETE (backlog B2) — cross-project meta-learning, opt-in/privacy-safe/read-only: new `globalLearning.enabled` (default OFF) + `vf_global_dir`/`vf_project_hash` helpers; when on, `consensus-aggregator.sh` (reverted/held) + `learning-apply` (applied) mirror **only** `{key, outcome, phase, projectHash}` (one-way sha256 — no code/content/file names/themes) to `~/.vibeflow/global-learning.jsonl` (overridable via `VIBEFLOW_GLOBAL_DIR`); `learning-loop-engine` consensus-history mode gains a `cross-project-signal` detector that aggregates per-key hold-rate across ≥3 distinct projects and **recommends** (≥0.7 allowlist / ≤0.3 avoid) — **never auto-allowlists or auto-applies**. Docs: `docs/CROSS-PROJECT-LEARNING.md`. v2.16.0 shipped 2026-06-05. Sprint 29 ✅ COMPLETE (backlog B5) — phase-runner TESTING auto-run: TESTING was the one phase that wasn't a one-command walk because `coverage-analyzer` parses an existing coverage JSON rather than producing it. New `phase-runner` Step 2a (TESTING only) runs the project's coverage command before forking the analyzers — resolved from `tech.coverageCommand` or defaulted by `tech.testRunner` (vitest→`npx vitest run --coverage`, jest, pytest, dotnet) — recorded as `generate:coverage` in the progress ledger, `allowed-tools` gains the runner binaries; best-effort (missing/failing command logs + continues so coverage-analyzer surfaces the gap), `--no-generate`/`VF_SKIP_GENERATE=1` opt-out, TESTING-only (other phase walks unchanged). Skill-only sprint. Docs: `docs/TESTING-READINESS.md`. v2.17.0 shipped 2026-06-05. Sprint 30 ✅ COMPLETE (backlog B4, **capstone**) — loop-status dashboard: `hooks/scripts/loop-audit.sh` gains `phaseRunner` (from the Sprint-21 progress ledger) + `crossProject` (from the Sprint-28 global store, only when `globalLearning.enabled`; both `null` when absent so `/vibeflow:flow-status` is unchanged); new read-only `/vibeflow:loop-status` skill consolidates phase-runner progress + auto-apply tally/revert-rates + cooldowns + armed watch + learning findings + the cross-project signal into one dashboard, flags `revertRate ≥ 0.5` removal candidates, surfaces cross-project as a recommendation-only signal, collapses to a single idle line when nothing has looped, and writes a durable `.vibeflow/reports/loop-status.md` report (registered in `phase-policy.json`, ALL, read-only). Skill + hook only. Docs: `docs/LOOP-STATUS.md` (new) + `docs/LOOP-AUDIT.md` refresh. v2.18.0 shipped 2026-06-05. **Backlog cleared — B1/B2/B4/B5 all shipped; the autonomous-loop arc is feature-complete. B3 (embedding excerption) is a deliberate non-goal.** Sprint 31 ✅ COMPLETE — onboarding clarity + a phase-runner that really runs (triggered by a real first-run session): (1) **BREAKING command rename** `/vibeflow:init` → `/vibeflow:onboard` (killed the collision with Claude Code's built-in `/init`; no shim — a shim would re-create it; skill registry + live docs + skills/hooks refs + sprint-13 sentinels repointed); (2) **`▶ Next:` breadcrumb convention** — onboard/prd-quality-analyzer/phase-runner end with the literal next command as their last output line, so `onboard → prd-quality-analyzer → phase-runner` is self-describing; (3) **phase-runner now drives consensus headlessly instead of deadlocking** — the consensus chain is all `disable-model-invocation: true` (un-forkable), so phase-runner used to deadlock against its own `consensus-gate`; new `hooks/scripts/consensus-run.sh` runs the codex/gemini reviewer CLIs as plain Bash and finalises a real `verdict.json` via the new additive `consensus-aggregator.sh --finalize` mode (no 600s quorum stall; same history.jsonl/reviewer-memory/auto-revert machinery; drains the marker), `consensus-gate.sh` allowlists it, and phase-runner records consensus + auto-advances on APPROVED or stops with an honest specialist→arbiter→apply breadcrumb on NEEDS_REVISION/REJECTED (deep rewrites stay operator-confirmed by design — Sprint 16 guardrail preserved because phase-runner is operator-invoked). The interactive `/vibeflow:consensus-orchestrator` (full 3-AI panel + iteration via claude-reviewer + SubagentStop aggregator) is **unchanged** — all sprint-14/15/16/17/19/23/28/30 suites stay green. Docs: `docs/ONBOARDING.md` (new) + PHASE-BOUNDARIES/CONSENSUS-FLOW refs. v2.19.0 shipped 2026-06-06. Sprint 32 ✅ COMPLETE — built-in command-collision audit + the second (and last) rename it found: a full sweep of all 41 skills against the Claude Code built-in slash-command set found `status` was the one remaining exact collision after `init`→`onboard` (it shadowed the built-in `/status` usage/quota), so **`/vibeflow:status` → `/vibeflow:flow-status`** (BREAKING command rename, no shim; 36 live refs repointed; `loop-status` reviewed + kept as a distinct name). Also removed CLAUDE.md's stale `/vibeflow:review` key command (no such skill → `/vibeflow:consensus-orchestrator`). Post-rename: **zero exact skill↔built-in collisions remain**; the audit + the naming policy for future skills live in `docs/COMMAND-COLLISIONS.md`. Skill-rename + docs only (no engine/hook logic change). v2.20.0 shipped 2026-06-06. v2.20.1 hotfix (2026-06-06) — `apply-arbiter-patch` now snapshots every target file to `.vibeflow/state/arbiter-backups/<session>/` before `git apply` (new Step 3.5) so rollback works off-git (REQUIREMENTS projects are often a bare PRD with no repo; the old `git checkout HEAD -- <file>` had nothing to restore from). Primary restore: `cp -R .vibeflow/state/arbiter-backups/<session>/. .`; git checkout stays secondary. Pinned by sprint-15.sh [S15-G]. v2.20.2 hotfix (2026-06-06) — reviewer rationale was silently dropped when codex/gemini wrap their verdict JSON in prose or a ```json fence (the FlowBridge/AntOS "codex NEEDS_REVISION with no rationale → specialist runs blind → never converges" trap): `append_cli_verdict` ran `fromjson` over the CLI's whole stdout, so surrounding prose made the parse fail → structured criticalIssues+suggestions collapsed to a bare keyword-grep verdict. New `vf_extract_json` (whole → strip fences → first-`{`…last-`}` span) unwraps it first in both `consensus-run.sh` and the orchestrator prose. Pure-prose still falls back to keyword grep; pure-JSON unchanged. Pinned by sprint-31.sh [S31-C] (static + fenced-codex runtime probe asserting suggestions/criticalItems captured). v2.20.3 hotfix (2026-06-07) — v2.20.2's greedy first-`{`…last-`}` span over-captured codex CLI 0.135.0's **duplicate-object** output (exec mode prints the verdict JSON twice with a `tokens used\nN` line between), swallowing both objects + junk → invalid JSON → rationale lost anyway. Replaced with a **balanced-brace first-object extractor** (`_vf_first_json_object`, awk depth-counter respecting braces-in-strings + escapes) in `consensus-run.sh` + orchestrator prose; takes only the first complete `{…}`. sprint-31.sh [S31-C] gains a duplicate-object runtime probe. Diagnosed in a live AntOS run. v2.20.4 hotfix (2026-06-07) — v2.20.3's "first balanced object" still failed on codex's **prompt-echo**: `consensus-run.sh` captures codex with `2>&1` and codex `exec` echoes the prompt, whose JSON **schema template** `{verdict:APPROVED|NEEDS_REVISION|REJECTED, …}` is the first balanced object but invalid JSON → extractor stopped there → 0/0 deterministically. `vf_extract_json` now walks **every** balanced object (`_vf_json_objects`, FS-delimited) and returns the first **valid** one **with a `.verdict` key** (skips the schema template). Once parsed, codex's real verdict was score-82 NEEDS_REVISION with 2 critical compliance gaps + 5 suggestions — never content-free. sprint-31.sh [S31-C] gains a prompt-echo runtime probe (→ 64/0). Diagnosed in a live AntOS run. Sprint 33 ✅ COMPLETE — **`consensus.cliTimeoutSeconds` config knob** (v2.21.0): the per-reviewer-CLI timeout was hardcoded at 90s; on a large primary (87KB PRD) a reviewer can exceed it and a timed-out reviewer is recorded as a conservative REJECTED that flips the aggregate. Now config-driven (ConsensusConfigSchema range 10–900, default 90, unit-tested; read by consensus-run.sh + orchestrator via vf_config_get). docs/CONFIGURATION.md + sprint-31.sh [S31-C] wiring sentinels (→ 68/0). v2.21.0 shipped 2026-06-07. Sprint 34 ✅ COMPLETE — **DESIGN-phase guide** (`/vibeflow:design-bootstrap`, v2.22.0): DESIGN was the only phase with no skill (phase-runner just said "operator writes design"). New skill authors the design artifacts the DESIGN panel reviews and — every run, no forced default — asks the operator which design source: **Claude-native** (zero-setup attractive+accessible UI spec + token system + wireframes from the PRD, domain-tuned), **Figma existing file** (pulled via the `design-bridge` MCP → tokens/styles; walks through `figma_token`/`FIGMA_TOKEN` setup), or **Figma from scratch** (official Figma MCP `use_figma`). All paths write `design/design-spec.md` (primary) + tokens + evidence, arm `consensus-needed.json`, and breadcrumb to `/vibeflow:phase-runner`; `design.approved`+`accessibility.verified` stay operator-confirmed. phase-policy DESIGN registration + phase-runner DESIGN row points at it. Docs: `docs/DESIGN-PHASE.md` (both Figma connection guides). sprint-34.sh (31/0). Also fixed a latent sprint-32 [S32-C] sentinel (bare-word `vibeflow:review` grep tripped by the ledger documenting its removal → scoped to the Key-Commands bullet). v2.22.0 shipped 2026-06-08. Sprint 35 ✅ COMPLETE — design-bootstrap refinements (v2.23.0), driven by running DESIGN on a UI product (AntOS) vs a backend increment (FlowBridge Faz 3): (1) **Step 1b UI-facing classification** — classifies UI vs backend/infra signals; backend-heavy increments get a menu flag that the UI options may not apply + heavy technical design belongs in ARCHITECTURE (still asks, never auto-decides); (2) **option 4 Both/compare** — produces BOTH a Claude-native and a Figma design into `design/candidates/`, writes a side-by-side `design/design-comparison.md` (+ `db_compare_impl` dimension check), operator picks → canonical `design/design-spec.md`; (3) **option 5 Technical design** — for low/no-UI increments, writes `design-spec.md` as a technical spec (with an ARCHITECTURE-handoff honesty check), no tokens/wireframes. Docs: `docs/DESIGN-PHASE.md`. sprint-35.sh (25/0). v2.23.0 shipped 2026-06-08. Sprint 36 ✅ COMPLETE — fair design comparison (v2.24.0), surfaced in a live AntOS compare-both: Claude produced ASCII wireframes while Figma produced `.png` exports, so Figma "looked nicer" because it was rendered, not because the design was better. Now the compare-both Claude candidate emits **self-contained HTML/CSS mockups** of the hero screens (token-driven inline CSS, real content+states, responsive+accessible) under `design/candidates/claude-native/mockups/`, screenshot-able to `.png` when a tool is available, so `design-comparison.md` is **rendered-vs-rendered**; the Claude-native path also prefers an HTML mockup per hero screen over an ASCII sketch. Also spelled out **what "merge" means** at the pick step (same design in two media → keep both, each in its role). Docs: `docs/DESIGN-PHASE.md`. sprint-36.sh (17/0). v2.24.0 shipped 2026-06-08. Sprint 37 ✅ COMPLETE — ARCHITECTURE author-side guide + phase-runner Skill-not-Agent fix (v2.25.0), from live AntOS + FlowBridge ARCHITECTURE walks: (1) new **`/vibeflow:architecture-bootstrap`** (counterpart to design-bootstrap) — `architecture-validator` only *validates* `docs/architecture.md` (hard precondition: doc exists) but nothing authored it, so greenfield ARCHITECTURE stalled; the skill asks the tech baseline (defaults/specify/stack-agnostic), authors `docs/architecture.md` + revisitable `docs/adr/ADR-NNN-*.md` with a **domain-policy-aware** security architecture (financial: at-rest encryption/PII-redaction/WORM-audit/RLS/NTP/secrets/kill-switch — the policies the validator checks), arms the consensus marker; (2) **phase-runner Step 2 fix** — said "Claude *forks* it", which led sessions to spawn a general/Explore agent instead of running the analyzer skill (returns exploration, not validation → policy gate silently bypassed); now explicit "run via the **Skill tool**, not a general/Explore agent". phase-policy + phase-runner ARCHITECTURE wiring. Docs: `docs/ARCHITECTURE-PHASE.md`. sprint-37.sh (30/0). v2.25.0 shipped 2026-06-08. Sprint 38 ✅ COMPLETE — the REAL root cause of "validator returned exploration not its report" (v2.26.0): **27 skills carried `agent: Explore` in frontmatter**, which runs them on the read-only Explore agent whose toolset **excludes Write** — so a skill declaring `allowed-tools: Read Write …` to produce a report literally couldn't write, fell back to a codebase summary, and its auto-satisfy/policy-gate never ran (phase-gate silently bypassed). v2.25.0 misdiagnosed it as the model spawning an agent; the routing was in the skill's own frontmatter. Confirmed in a live AntOS run where architecture-validator was invoked via the **Skill tool** and still returned exploration. **Removed `agent: Explore` from all 27 skills** (kept `context: fork` — apply-arbiter-patch proves a forked skill writes fine without Explore). Regression guard: sprint-38.sh asserts no skill carries `agent: Explore` (11/0). v2.26.0 shipped 2026-06-08. Sprint 39 ✅ COMPLETE — **brownfield on-ramp** (`/vibeflow:brownfield-intake`, v2.27.0): the SDLC starts at REQUIREMENTS (assumes a PRD), but brownfield projects have code + do an *increment*, not a whole product. New skill fingerprints the repo (codebase-intel) so you don't explain the architecture, asks how to define the work **4 ways** (describe / point-to-doc / guided Q&A / GitHub-GitLab issue via gh/glab), and synthesizes a **code-anchored, increment-scoped** requirements doc (`docs/<increment>-requirements.md`) with a UI/backend/infra **change-type classification** that routes the later phases (backend → DESIGN light + architecture incremental; UI → DESIGN central) → prd-quality-analyzer → phase-runner. `onboard` now branches: existing codebase → breadcrumbs to brownfield-intake (not prd-quality-analyzer). phase-policy registration. Docs: `docs/BROWNFIELD.md`. sprint-39.sh (32/0). Formalizes what FlowBridge did by hand (a backend Faz-3 increment on a branch). v2.27.0 shipped 2026-06-08. Sprint 40 ✅ COMPLETE — **artifact archive before rewrite** (v2.28.0): `apply-arbiter-patch` (the single point a framework-driven rewrite touches the PRD — the prd-rewriter specialist is diff-first, never writes the doc) now, in Step 3.5 **before** `git apply`, copies each target file to a **persistent, timestamped** archive `.vibeflow/archive/<path>.<UTC-ts>.bak` — a discoverable version trail ("what did the PRD look like before round 2?"), distinct from the session rollback snapshot (`.vibeflow/state/arbiter-backups/<session>/`, kept for one-command undo). Covers every artifact a patch targets (PRD/design-spec/architecture/ADRs). sprint-40.sh (16/0). v2.28.0 shipped 2026-06-08. Sprint 41 ✅ COMPLETE — DEVELOPMENT walk fixes (v2.29.0), from a live AntOS DEVELOPMENT run: (1) **3 auto-satisfy analyzers were `disable-model-invocation: true`** (`quality-gates` DEVELOPMENT; `release-decision-engine` + `deploy-verifier` DEPLOYMENT) → phase-runner couldn't fork them, operator ran lint/typecheck/tests by hand (quality-gates' own description says "without manual MCP calls" — direct contradiction). Removed it from all three; human stays in control via the visible decision + consensus.<phase>.approved gates + the manual deploy. Same class as the v2.26.0 agent:Explore fix. (2) **phase-runner DEVELOPMENT diff was hardcoded `git diff HEAD~1..HEAD`** → exits 128 on non-git / <2 commits, and only reviewed the last commit; now resolves the base robustly (git-repo guard + merge-base with default branch for whole-increment review + HEAD~1 fallback + empty-tree for the first commit). sprint-41.sh (16/0). Also fixed a latent Sprint-33 sprint-26 [S26-F] sentinel (asserted literal `vf_run_timeout 90 gemini` after the timeout went config-driven). v2.29.0 shipped 2026-06-08. Sprint 42 ✅ COMPLETE — **project lifecycle (cycles/resume/increments)** (v2.30.0), from an operator who paused a greenfield project and didn't want a brownfield re-intake on resume: VibeFlow now tracks a project as a sequence of **cycles** (one SDLC pass per deliverable) in `.vibeflow/state/lifecycle.json` — cycle-1 is the initial build (greenfield/brownfield is a cycle-1-only distinction), every later cycle is an increment. The runtime question is "is there an **open cycle to resume**, or did the last one **ship**?" — read from cycle status, NOT file presence. `onboard` gained a **Step 0 resume-guard** (already-onboarded → routes to phase-runner [resume] or brownfield-intake [new increment if last cycle shipped], never re-onboards; **fixes** a paused greenfield-with-source being misread as brownfield). `phase-runner` **Step 0** reads lifecycle (in-progress→resume / completed→new increment on a fresh branch) and **DEPLOYMENT closes the cycle** on GO (release.decision.go + deployment.verified + consensus.deployment.approved → completed + history[]); not on CONDITIONAL/BLOCKED. **One cycle = one branch/PR** (track-and-breadcrumb — VibeFlow records branch/prUrl + suggests git/gh, operator runs them). brownfield-intake opens an increment cycle. Docs: `docs/LIFECYCLE.md`. sprint-42.sh (28/0). v2.30.0 shipped 2026-06-08. Sprint 43 ✅ COMPLETE — **arm consensus only on PASS** (v2.31.0), from a live AntOS TESTING run: `coverage-analyzer` + `mutation-test-runner` wrote `consensus-needed.json` **unconditionally** (even on NEEDS_REVISION/BLOCKED) → `consensus-gate` then blocked every subsequent Bash|Write|Edit → blocked the very iteration needed to FIX the failure (add tests, install Stryker) → operator had to repeatedly `VF_SKIP_CONSENSUS_GATE=1` + `rm` the marker (defeats the gate). Both now arm the marker **only when VERDICT==PASS**; non-PASS emits the gap advisory + ▶ Next fix breadcrumb and leaves the gate clear. Matches the existing marker-less `quality-gates` precedent; marker schema unchanged on the PASS path. sprint-43.sh (13/0); sprint-18/19 green. v2.31.0 shipped 2026-06-08. Sprint 44 ✅ COMPLETE — **guaranteed UI-conditional front-end test battery** (v2.32.0): coverage+mutation gate test-quality, not whether the UI is right. New **`/vibeflow:input-validation-matrix`** builds a systematic per-field data-validation matrix (required/type/boundary[value+length]/format[IBAN/email/precision]/type-mismatch/injection-safety[XSS·SQL escaped-or-rejected, hard-BLOCKED for financial/healthcare]/output-formatting/cross-field) and writes runnable tests **into the project suite** (so coverage + regression/quality-gates enforce them) + `validation-matrix.md`. `phase-runner` TESTING now orchestrates the full **front-end battery only when the increment is UI-facing** (change-type ui/mixed or design-spec.md exists): input-validation-matrix + e2e-test-writer (functions work, Playwright/Detox) + business-rule-validator (meet requirements) + traceability-engine (every UI req tested) + visual-ai-analyzer (design conformance) + uat-executor (staging E2E); backend/infra increments skip it. The tests-in-suite + battery-reports-as-consensus.testing.approved-evidence make it a real gate (UI increment can't reach GO without it green). A/B is deliberately out of scope (production experiment → observability MCP). phase-policy registration. Docs: `docs/FRONTEND-TESTING.md`. sprint-44.sh (34/0). v2.32.0 shipped 2026-06-08. Sprint 45 ✅ COMPLETE — **mobile crash & stability testing** (`/vibeflow:mobile-stability-runner`, Expo-aware, v2.33.0): functional E2E (Detox) catches flow failures but mobile/Expo crashes hit at **cold start** (native-module/app.config/env mismatch) or lifecycle events, not the happy path. New skill **auto-detects the runner** (Expo→Maestro on Expo Go/dev-client / bare RN→Detox), runs a **crash-focused battery** (cold-start smoke + background↔foreground + low-memory + deep-links + permission-denial + network-loss + rotation + JS unhandled-rejection), detects native-crash/redbox/ANR + surfaces crash-reporter (Sentry/Crashlytics/Expo) captures. **Graceful-degrade**: no simulator/device → NEEDS_REVISION + local-run breadcrumb (not a false fail); crash on cold-start/P0-flow → hard BLOCKED. phase-runner TESTING runs it **when platform is ios/android/all** (alongside the front-end battery); non-mobile skips. arm-on-pass. phase-policy registration. Docs: `docs/MOBILE-TESTING.md`. sprint-45.sh (32/0). v2.33.0 shipped 2026-06-08. Sprint 46 ✅ COMPLETE — **lifecycle completion clarity** (v2.34.0), from a live ANTOS run that merged Sprint-1's PR, parked at DEPLOYMENT in-progress, and was nudged toward a brownfield re-intake on a still-unfinished greenfield project: the Sprint-42 state machine was **correct** (phase-runner resumed the in-progress cycle, didn't treat it as brownfield) — the bug was the model's **narrative** conflating "PR merged + reached DEPLOYMENT" with "cycle shipped → next increment." A merged PR / reaching the last phase is **not** completion; only **DEPLOYMENT GO** flips status:completed. phase-runner Step 0 + DEPLOYMENT-close note + onboard Step 0 + docs/LIFECYCLE.md now say this loudly + add the **stuck-DEPLOYMENT** case (no CI/deploy/health infra → resume+finish OR operator explicitly closes/defers; never auto-start a new increment, which silently abandons the open cycle). Guidance-only (no engine change). sprint-46.sh (17/0). v2.34.0 shipped 2026-06-08. Sprint 47 ✅ COMPLETE — **the recurring consensus-run.sh parser bug, landed for good** (v2.35.0), from a live ANTOS diagnosis: `consensus-run.sh` + orchestrator captured codex/gemini with **`2>&1`**, merging stderr into the verdict blob. codex `exec` echoes the prompt (with the JSON schema template `{verdict:APPROVED|NEEDS_REVISION|REJECTED,…}`, invalid JSON) + banner to **stderr**; on a large/interleaved diff that template defeated vf_extract_json → keyword-grep fallback (REJECTED checked first, matched in the template) → codex recorded as a **content-free REJECTED** that flipped the aggregate + pinned agreement 0.5 (specialist ran blind, never converged). Root-cause fix: **capture stdout-only** (`2>"$errfile"`, stderr to a separate file for warn_cli_error). Defense-in-depth: keyword-grep drops the `|`-alternation schema-template line before classifying. v2.20.4 walk-all-objects extractor (first valid object with a `.verdict` key) kept. Applied to consensus-run.sh (303/316) + orchestrator prose (codex+gemini). The prior v2.20.2/.3/.4 chased the *extraction* side; this fixes the *capture* that fed it poison. sprint-47.sh (19/0, static + runtime probes); sprint-14/15/31 green. v2.35.0 shipped 2026-06-08. Sprint 48 ✅ COMPLETE — **multi-cycle lifecycle gets its engine reset** (`sdlc_start_cycle`, v2.36.0), from a live ANTOS cycle-2: Sprint 42 added per-cycle `lifecycle.json` tracking but the `sdlc-engine` MCP models one linear pass + stayed pinned at the prior cycle's terminal phase (DEPLOYMENT) → opening cycle-2 left engine=DEPLOYMENT vs config=REQUIREMENTS → phase-runner phase-mismatch (no reset; backward `advance_phase` correctly rejected). New `sdlc_start_cycle` engine tool resets currentPhase→REQUIREMENTS + clears criteria/consensus + bumps `ProjectState.cycle` (an explicit audited cycle boundary, not a backward rewind). `brownfield-intake` calls it when opening a new increment; `phase-runner` Step 0 reconciles a pre-existing drift (engine ahead of the cycle). ProjectState.cycle optional/back-compat. Engine unit tests 200→201. Docs: `docs/LIFECYCLE.md`. sprint-48.sh (22/0). v2.36.0 shipped 2026-06-08. Sprint 49 ✅ COMPLETE — **HTML-escaped criterion no longer blocks the gate** (v2.37.0), from a live ANTOS cycle-2 advance: exit criteria like `testability.score>=60` contain `>`; a caller (model/serialization) HTML-escaped the name so `sdlc_satisfy_criterion` stored `testability.score&gt;=60`, which never matched the literal exit criterion → advance blocked **despite "satisfied"** (bites any criterion with `>`/`<`/`&`). Engine now **normalizes HTML entities** on the single write path (`normalizeCriterion`: &gt;→> / &lt;→< / &amp;→& decoded last / quotes / trim), so the stored set is the canonical literal. Exported + unit-tested; engine 201→202. sprint-49.sh (11/0, static + live node probe). v2.37.0 shipped 2026-06-08. Sprint 50 ✅ COMPLETE — **consensus-gate escape hatch works with two env vars** (v2.38.0), from a live ANTOS ARCHITECTURE run: `consensus-gate.sh` only checked `VF_SKIP_CONSENSUS_GATE` as an exported env var, and Claude Code reliably exports a single leading `VAR=val` but not two → `VF_SKIP_CONSENSUS_GATE=1 VF_ALLOW_PHASE_WRITE=1 bash -c …` was blocked while the single-var form worked (operator found it by trial). Gate now also parses the **leading env-var prefix** of the command string (awk extracts the run of `VAR=val` tokens before the first command word) and honors the bypass there — order/count-independent — while a mention inside a quoted string arg does NOT bypass + a normal command with an armed marker still blocks. env check kept. sprint-50.sh (11/0, static + runtime probes). v2.38.0 shipped 2026-06-08. Sprint 51 ✅ COMPLETE — **phase-write-guard monorepo-aware source paths** (v2.39.0), from a live ANTOS (monorepo) TESTING run: the source `warn` glob was `src/**` (top-level only), so a monorepo source file (`packages/*/src/…`, `apps/*/src/…`) matched neither allow nor warn → fell to a **hard block** in TESTING/DEPLOYMENT, even though those phases *intend* a warn (soft-allow) on source — it bit a legitimate Stryker `// Stryker disable next-line` equivalent-mutant annotation (operator had to VF_ALLOW_PHASE_WRITE=1). TESTING + DEPLOYMENT `warn` now include `**/src/**` alongside `src/**`; monorepo source → warn, non-src `lib/` → still block, REQUIREMENTS source → still block (no regression). Test patterns (`**/*.test.*`) were already monorepo-aware; this brings source into line. sprint-51.sh (15/0, static + matcher runtime probe). Docs: `docs/PHASE-BOUNDARIES.md`. v2.39.0 shipped 2026-06-09. v2.39.1 (2026-06-09) — maintainer contact email metadata update (plugin.json/marketplace.json), no functional change. Sprint 52 ✅ COMPLETE — **phase-runner surfaces in-flight work before progressing** (v2.40.0), from an operator who ran phase-runner expecting it to also handle a pending commit: phase-runner only advances the SDLC *phase* (doesn't commit/push), and could silently advance past **uncommitted work** that the phase step won't see — worst going into DEVELOPMENT, whose `code.reviewed` runs consensus on the **committed** diff (uncommitted = unreviewed). Step 0 now checks `git status --porcelain` + `review-pending.json` and **names** what's pending with a heads-up + git add/commit breadcrumb. Surface-only: never auto-commits (git stays the operator's), never blocks, quiet when nothing pending; allowed-tools add read-only `git status`/`git rev-parse`. sprint-52.sh (11/0). v2.40.0 shipped 2026-06-09. Sprint 53 ✅ COMPLETE — **frontend-render-check: actually render the UI + compare to the design** (v2.41.0), from a live ANTOS finding that the implemented screen had **no relation** to its Figma design + the dev server wouldn't boot on a path-with-spaces (`%20` alias) — neither caught by the test suite (the "dark tunnel": UI ships unit-tested but never rendered). New skill boots the web app (framework-detect; won't-boot is a finding), serves **mock data** (no backend), **captures + surfaces** hero-screen screenshots (you SEE it + the localhost URL for Chrome), and compares **implemented vs designed** (Figma via `db_compare_impl` / design-spec mockups + tokens) side-by-side; BLOCKED on won't-boot or no-relation-to-design. Uses mocks on purpose (TESTING); live front+back stays `uat-executor` (DEPLOYMENT). Runs **first** in the phase-runner TESTING front-end battery (feeds visual-ai-analyzer); arm-on-pass; graceful-degrade. phase-policy registration. Docs: `docs/FRONTEND-TESTING.md`. sprint-53.sh (29/0). v2.41.0 shipped 2026-06-09. Sprint 54 ✅ COMPLETE — **"UI never render-verified" debt signal** (v2.42.0), from realising ANTOS's dashboard (built cycle-3, before the visual tooling existed) had never once been rendered or compared to its Figma design + stayed buried under later backend cycles (the front-end battery is increment-conditional, so an earlier-cycle UI is never re-checked). New read-only `hooks/scripts/ui-verification-debt.sh` detects a repo with a web UI (framework dep + web entry) but no `frontend-conformance.md` (`never`) or one older than the UI source (`stale`); `phase-runner` Step 0 + `flow-status` surface `⚠ … → /vibeflow:frontend-render-check` — surface-only (never blocks/auto-runs, silent on backend-only/verified). sprint-54.sh (20/0, static + runtime probes). v2.42.0 shipped 2026-06-10. Sprint 55 ✅ COMPLETE — **catch a "skinless" UI in DEVELOPMENT** (v2.43.0), from a live ANTOS render-check: the dashboard was built with the right DOM/content/a11y/class-names but **zero CSS** (no stylesheet/Tailwind/CSS-in-JS, design-tokens.json never consumed) → compiled, passed lint/typecheck/tests, read clean in the diff (an **absence** is invisible to quality-gates + code.reviewed), rendered as bare HTML. New read-only `hooks/scripts/ui-styling-check.sh` classifies a web UI `skinless`/`styled`/`no-ui`; `phase-runner` DEVELOPMENT surfaces `skinless` **prominently** (design specified but never implemented → implement styling before TESTING), and `frontend-render-check` runs the same check as an immediate hard BLOCKED (nameable without a browser). Surface-only in DEVELOPMENT. sprint-55.sh (20/0, static + runtime probes). v2.43.0 shipped 2026-06-10. Sprint 56 ✅ COMPLETE — **phase-runner setup stops tripping its own consensus-gate** (v2.44.0), from a live ANTOS cycle-6 run: an analyzer (prd-quality-analyzer) arms `consensus-needed.json` just before phase-runner runs → `consensus-gate` blocks every Bash → phase-runner's read-only **setup** (lifecycle read, git status, the UI readers) threw a hook error on its first action, forcing a VF_SKIP dance. Fix: (1) phase-runner Step 0 reads lifecycle via the **Read tool** + phase via **MCP** (ungated), not `cat`/`jq`; (2) `consensus-gate` allowlists VibeFlow's read-only readers (ui-verification-debt/ui-styling-check/loop-audit) + `git status`/`git rev-parse` (inspect-only). A **write** or forward work still BLOCKs (gate purpose intact). sprint-56.sh (19/0). v2.44.0 shipped 2026-06-10. Sprint 57 ✅ COMPLETE — **UI test scenarios enumerated up front in PLANNING** (v2.45.0), from an operator's general process question (do we enumerate detailed test scenarios — every field, both how it *looks* and how it behaves with *real backend data* — up front, AI-review them, then use them in test runs?): functional scenarios were already up-front + AI-reviewed + used downstream, but the **UI surface** (per-field visual conformance, per-field validation, backend-data binding) was only generated *inside* TESTING — never part of the reviewed PLANNING deliverable, never RTM-traced, leaving the front-end battery to improvise. `test-strategy-planner` gains **Step 2b: UI Scenario Enumeration** (UI-conditional — platform web/all **and** a `design/design-spec.md` exists via Glob; skipped+recorded for backend/infra so they're unchanged): reads the design spec and, per hero screen/field, enumerates **visual-conformance** (`SCN-UI-<screen>-V-NN` — color/font/spacing/layout + default/hover/focus/disabled/error/loading states), **field-validation** (`SCN-UI-<screen>-VAL-NN` — required/type/boundary/format), and **backend-data-binding** (`SCN-UI-<screen>-DATA-NN` — correct server value + loading/populated/empty/error/offline) into a `## UI Scenarios` block in `scenario-set.md`. Every UI scenario is traced back to the functional requirement its screen serves so the RTM carries the `FR-* → screen → UI scenario` chain; coverage-gap gains UI_UNTESTED/UI_DATA_GAP/DESIGN_REQ_GAP (a stray screen is a finding, never a silently invented requirement). These become the contract the TESTING front-end battery implements (frontend-render-check/input-validation-matrix/visual-ai-analyzer/uat-executor) — the up-front, multi-AI-reviewed scenario doc is now honest about the UI surface instead of deferring all per-field visual/data checks to TESTING. Skill-only change (no engine/hook logic). sprint-57.sh (31/0). v2.45.0 shipped 2026-06-12. Sprint 58 ✅ COMPLETE — **full-stack increments must be integration-verified with real test data, not mocked** (v2.46.0), from an operator who found a cycle shipped mock front-ends over a backend that was only *claimed* to exist: the front↔back integration was only ever checked by `uat-executor` against a **deployed staging** env most projects never stand up (a backend with no HTTP server can't reach staging at all → integration *never* verified). New **`integration-verifier`** skill (TESTING, full-stack-only) boots the **real** back-end **locally** (no deploy; no-HTTP-server is itself the BLOCKED finding), seeds deterministic data via `test-data-manager`, points the **real** front-end at it off mocks, and drives the Sprint-57 `SCN-UI-*-DATA-*` data-binding scenarios end-to-end (populated/loading/empty/error/offline) + `curl`s each endpoint for a 404; arm-on-pass. New read-only **`hooks/scripts/integration-wiring-check.sh`** (sibling of `ui-styling-check.sh`) classifies the boundary `wired`/`unwired` ("the links have no backend behind them")/`single-tier` (the front-end-only / back-end-only carve-out)/`no-app`, splitting FE vs BE source dirs by nearest package.json so an axios `.get(` in the UI isn't read as a server route. `phase-runner` runs it as the middle integration rung in TESTING (between the mock render and the staging UAT, detector-gated) and surfaces an `unwired` UI **prominently** in DEVELOPMENT (caught while built, like Sprint 55's skinless; hard gate stays in TESTING). Docs: `docs/INTEGRATION-TESTING.md` (new — the three rungs + the carve-out + "a *claimed* backend is not a *verified* backend") + `docs/FRONTEND-TESTING.md` three-rung refresh. Skill + hook + docs only (no engine/MCP change). sprint-58.sh (47/0); run.sh + sprint-53/54/55/57 green. v2.46.0 shipped 2026-06-14 (PR #3 merged + GitHub release v2.46.0 Latest). Sprint 59 ✅ COMPLETE — **the front↔back integration gate is now stack-agnostic** (v2.47.0), from a live Clera session whose **Python/FastAPI** backend Sprint 58's JS/TS-only gate couldn't even see (it read `no-app`/`single-tier`, so the gate stayed dormant — the very "backend claimed working, never seen running with a UI" case it exists to catch). `integration-wiring-check.sh` now detects the back-end **manifest-first** across Node/Python/Go/.NET/Java with per-language route-handler patterns scoped to each stack's dirs (an axios `.get(` in the UI is still never read as a server route), and the front-end is **framework-optional** (a plain `index.html`+`fetch(` counts; a static design mockup with no calls stays `single-tier`); same four-status contract, fail-safe, surface-only. `integration-verifier` boots the back-end across stacks (Python `uvicorn`/Go `go run`/.NET `dotnet run`/Java `mvnw`·`gradlew`, Node unchanged) with `VF_BACKEND_CMD`/`VF_BACKEND_PORT` overrides; a no-HTTP-server library (the Clera shape — a Python engine with no FastAPI/uvicorn entry) stays an explicit BLOCKED finding regardless of language. phase-runner stack-agnostic note + `docs/INTEGRATION-TESTING.md` Supported-stacks matrix. Skill + hook + docs only (no engine/MCP change). sprint-59.sh (33/0); sprint-58.sh (47/0) + run.sh (399/0) green. v2.47.0 shipped 2026-06-15 (PR #4 merged). Sprint 60 ✅ COMPLETE — **parallel team work via per-branch work-streams** (opt-in `streams.enabled`, default OFF): a branch-derived `projectId` (`<project>__<slug>`, via `vf_stream_id`/`vf_lifecycle_path` + the read-only `stream-id.sh`/`streams-audit.sh` readers) gives each git branch/worktree its own isolated `.vibeflow/state/<id>/` so two developers progress independent SDLC state that never clobbers each other — the `sdlc-engine` already locks+isolates per projectId, so **no engine logic change** (only a new optional `StreamsConfigSchema`); default branch/detached/non-git collapse to the bare id so existing single-stream projects are bit-for-bit unchanged. New `/vibeflow:streams` dashboard + `/vibeflow:integrate` merge point (integration cycle + `integration-verifier` over the combined surface + cross-feature consensus). Docs: `docs/TEAM-WORK.md`. sprint-60.sh (63/0); hooks 185→198, engine 202→206, run.sh 399/0 green. Merged via **PR #5** (commit/release version-bump pending). Sprint 61 ✅ COMPLETE — **operator decisions become tappable choices** (default-on, mobile/remote-first): at every operator-blocking decision point nine skills (onboard/brownfield-intake/design-bootstrap/architecture-bootstrap/apply-arbiter-patch/phase-runner/integrate/release-decision-engine/learning-apply) surface 2–4 options via the **`AskUserQuestion`** tool (recommended first, condensed rationale per option) instead of a prose menu / `[y/N]` the operator must read+type; the built-in "Other" always preserves manual typing, so it's pure upside (no config flag) and much faster to drive from a phone. Prose menu kept as fallback. Docs: `docs/OPERATOR-CHOICES.md`. sprint-61.sh (57/0); pure skill-prose + frontmatter + docs (no engine/hook/MCP change). Merged via **PR #5**. v2.48.0 shipped 2026-06-23 (PR #5 + GitHub release; also restored a working-tree consensus-run.sh regression that reverted Sprint 47, removed release.sh's stale postgres-era pg peer-dep pre-flight that blocked releases, and hardened hooks/tests/run.sh against ambient VF_ALLOW_PHASE_WRITE/VF_SKIP_CONSENSUS_GATE leakage). Sprint 62 ✅ COMPLETE — **consensus-gate honors the bypass after a leading `cd` preamble** (v2.49.0): the awk env-prefix scan stopped at `cd`, so `cd /repo && VF_SKIP_CONSENSUS_GATE=1 cmd` was blocked while a leading-assignment form worked (diagnosed in a live FlowBridge_TR ARCHITECTURE session polishing docs/architecture.md before consensus). `consensus-gate.sh` now strips leading `cd … (&&|;)` segments before the scan — only `cd` (navigation, not work) is strippable, and a quoted mention / non-cd work ahead of the override still blocks. Hook-only change. sprint-62.sh (16/0); hooks 198→206. v2.49.0 shipped 2026-06-23 (PR-less direct release on main + GitHub release). Sprint 63 ✅ COMPLETE — **read-only views surface their next-step as a tappable choice** (v2.50.0), from a live FlowBridge_TR run where `/vibeflow:flow-status` ended with a prose "Next step: run /vibeflow:advance to move TESTING → DEPLOYMENT" instead of a tappable option (flow-status was a read-only reporter, not in the Sprint-61 nine). `flow-status` / `loop-status` / `streams` now present their closing next action via `AskUserQuestion` (recommended first, always a "Stay / do nothing" option, "Other" to type, card omitted when nothing's actionable). Extends the Sprint-61 convention to terminal next-step breadcrumbs. docs/OPERATOR-CHOICES.md refreshed. sprint-63.sh (30/0). Skill-prose + docs only. v2.50.0 shipped 2026-06-23 (direct release on main + GitHub release). Sprint 64 ✅ COMPLETE — **deterministic single-step `▶ Next:` breadcrumbs become tappable "Run now / Not yet" cards** (v2.51.0): the 13 breadcrumb-emitting skills now present their closing `▶ Next: <command>` handoff as a tappable AskUserQuestion (Run-now Recommended / Not yet; Other to type; breadcrumb kept as fallback) so the whole SDLC can be walked phone-first, one tap per handoff. Also fixed 4 latent buggy `allowed-tools:[^\n]*` test sentinels (the `[^\n]` matched "not backslash-or-n", which a literal `n` in `AskUserQuestion` defeated → `.*`). docs/OPERATOR-CHOICES.md refreshed. sprint-64.sh (62/0); full set green except the 4 known pre-existing (sprint-8/11/20/24). Skill-prose + docs + test-sentinel fixes. v2.51.0 shipped 2026-06-23. Sprint 65 ✅ COMPLETE — **ambient operator-choice convention on SessionStart** (v2.52.0), from a live FlowBridge_TR remote-control screen where the model asked a strategy choice ("build the API layer first, or go straight to DEVELOPMENT? which one?") in **prose** instead of tappable options — because that question is ad-hoc orchestration *outside any skill*, so the Sprint 61/63/64 skill-point cards didn't govern it. `load-sdlc-context.sh` (the SessionStart hook) now injects a one-line convention nudging the whole session to render **any** operator choice/confirmation via `AskUserQuestion` (best-effort session-wide hint vs the hard skill-point guarantees). For a hard guarantee a consumer can also add the same line to its own CLAUDE.md. sprint-65.sh (11/0); hooks 206→208; load-sdlc-context budget 250→450. v2.52.0 shipped 2026-06-23. Sprint 66 ✅ COMPLETE — **the visual battery is stack-agnostic** (v2.53.0), from a live Clera review where the operator manually spotted visual defects (spacing/₺-glyph/empty-state/dağınık layout) on a budget-policy screen that VibeFlow's own frontend-render-check + visual-ai-analyzer never caught — because UI detection was JS-framework-only, so Clera's FastAPI HTML-response SPA (no package.json framework dep) read as no-ui and the entire visual battery silently self-skipped (the visual-layer twin of Sprint 59's integration-gate gap). New vf_web_ui_kind makes detection stack-agnostic (js/server-rendered/static/none); ui-styling-check/ui-verification-debt now see server-rendered UIs (Clera → "never" = web UI present but never render-verified, surfaced by phase-runner/flow-status); frontend-render-check gains a server-rendered path that boots the REAL server (uvicorn/flask/django/dotnet/rails) and screenshots its routes for visual-ai-analyzer. Hook + skill + docs only. sprint-66.sh (26/0); hooks 208→213; sprint-54/55 green.
- **Bonus (not in baseline):** `examples/demo-app/` ships 45 vitest tests and `examples/nextjs-demo/` ships **66** vitest tests (41 from S5-05 + 25 from S6-04 rating helpers) — run with `cd examples/<demo> && npm install && npm test`. The Next.js demo also supports `npm run build` (production build); `sprint-6.sh [S6-B]` auto-runs it when `examples/nextjs-demo/node_modules/next` is present and `VF_SKIP_NEXT_BUILD` is unset.

## Coding Conventions
- TypeScript for all MCP servers and scripts
- Zod schemas for all MCP tool input validation
- Enum-safe comparisons (never string literals for status values)
- All skill outputs follow the explainability contract: { finding, why, impact, confidence }
- **Operator decisions are tappable choices, not typed prose** (Sprint 61): at any operator-blocking decision point a skill uses the `AskUserQuestion` tool to surface 2–4 options (recommended first, condensed rationale in each description); the built-in "Other" always preserves manual typing. Mobile/remote-first. See `docs/OPERATOR-CHOICES.md`.

## Sprint Tracking
- Full roadmap: `ROADMAP.md` (4 sprints, skill inventory, bug tracker)
- Active sprint: the highest-numbered file in `docs/SPRINT-*.md` that is not yet marked ✅ COMPLETE
- When starting a new session, read the active sprint file to find the next ticket (look for "Next Ticket to Work On")
- When completing a ticket, tick its checkbox in the active sprint file
- When all Sprint N tickets are done, mark that file ✅ COMPLETE and create `docs/SPRINT-{N+1}.md` seeded from ROADMAP.md. Do not leave the previous sprint file as the active pointer.
- `docs/SPRINT-*.md` files are auto-written by `hooks/scripts/save-plan-doc.sh` on every `ExitPlanMode` (v2.6.1). A plan titled `# Sprint NN — …` lands at `docs/SPRINT-NN.md`; non-sprint plans fall back to `docs/plans/<slug>-<date>.md`. Sprints 14-18 were backfilled from release-notes on 2026-04-22 as part of the same patch.

## File Naming Conventions
- Skills: `skills/<name>/SKILL.md` (YAML frontmatter with `name:` + `description:` is mandatory — the integration harness will fail if either is missing)
- Subagents: `agents/<name>.md`
- Hook scripts: `hooks/scripts/<name>.sh` — all hooks source `hooks/scripts/_lib.sh` for shared helpers (`vf_current_phase`, `vf_mode`, `vf_phase_index`, …). Never duplicate config/state reads; extend the lib instead.
- Hook tests: `hooks/tests/run.sh` — bash 3.2 compatible (no associative arrays; default macOS shell is the lowest common denominator)
- MCP servers: `mcp-servers/<name>/src/index.ts`, tests in `mcp-servers/<name>/tests/*.test.ts`
- Integration harness: `tests/integration/run.sh` — covers anything that crosses process boundaries or validates static manifests
- Sprint plans: `docs/SPRINT-{N}.md`
